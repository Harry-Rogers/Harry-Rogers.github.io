---
phase: 01-bootstrap-deploy
plan: 04
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified: []
autonomous: false
requirements: [DEPLOY-01, DEPLOY-03]
must_haves:
  truths:
    - "Local main is force-pushed to remote origin/main, replacing the stale 2020 history"
    - "GitHub Pages source is set to: branch=main, folder=root (`/`)"
    - "The most recent pages-build-deployment workflow run is green"
    - "`curl -sI https://harry-rogers.github.io/` returns HTTP/2 200 with no project subpath"
    - "View-source of the homepage contains no `academicpages` references in canonical/og URLs"
  artifacts:
    - path: "(remote) origin/main"
      provides: "Force-pushed academicpages-based site replacing the 2020 hand-rolled HTML history"
    - path: "(GitHub Pages settings)"
      provides: "Source = main branch, root directory; default Jekyll builder enabled"
  key_links:
    - from: "local main"
      to: "remote origin/main"
      via: "git push --force"
      pattern: "git ls-remote origin main"
    - from: "GitHub Pages"
      to: "main branch root"
      via: "Settings -> Pages (UI) or `gh api -X PUT repos/.../pages`"
      pattern: "gh api repos/Harry-Rogers/Harry-Rogers.github.io/pages | jq -r .source.branch"
---

<objective>
Add `origin`, destroy the remote 2020 history via force-push to `main`, set the GitHub Pages source to `main`/root, wait for the default Pages builder to ship a green deployment, and verify the canonical URL `https://harry-rogers.github.io/` returns HTTP 200 with no project subpath. This satisfies Bootstrap Gates G1 (canonical URL at root) and G3 (`main` is the Pages source, last build green), and operationally completes DEPLOY-01 (force-overwrite live remote) and DEPLOY-03 (Pages source = main/root).

Purpose: Per CONTEXT.md locked decision 3 ("Old remote history = force-push, discard entirely. No `legacy-2020` tag/branch.") and locked decisions 13, 14: setting Pages source is an owner action via Settings → Pages; force-push must be confirmed by owner. Plan is `autonomous: false` end-to-end because two steps require the owner: (1) confirming the destructive force-push, (2) toggling the Pages source if `gh` lacks repo:admin scope locally.

Output: Remote `main` reflects the local academicpages-based site, Pages serves a green build, and the canonical URL resolves with no subpath and no academicpages references.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-bootstrap-deploy/01-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add origin remote and verify it points at the correct repo</name>
  <files>
    - (none — modifies .git/config only)
  </files>
  <read_first>
    - .planning/phases/01-bootstrap-deploy/01-CONTEXT.md ("The remote `Harry-Rogers/Harry-Rogers.github.io` exists and serves a stale 2020 hand-rolled HTML/CSS site. Must be added as `origin` and force-pushed.")
  </read_first>
  <action>
    1. If `git remote -v` already lists `origin`, verify the URL matches `https://github.com/Harry-Rogers/Harry-Rogers.github.io.git` (HTTPS) or `git@github.com:Harry-Rogers/Harry-Rogers.github.io.git` (SSH). If mismatched, run `git remote set-url origin <correct>`.
    2. If `origin` is missing, run: `git remote add origin https://github.com/Harry-Rogers/Harry-Rogers.github.io.git` (HTTPS by default; owner may prefer SSH — if so, use `git@github.com:Harry-Rogers/Harry-Rogers.github.io.git`).
    3. Confirm: `git remote -v` prints two lines (fetch + push) for `origin` pointing at the canonical repo.
    4. Fetch only (do NOT pull, do NOT merge — the histories are unrelated): `git fetch origin`. This populates `origin/main` ref locally but does not modify the working tree.
  </action>
  <verify>
    <automated>git remote get-url origin | grep -qE '^(https://github\.com/Harry-Rogers/Harry-Rogers\.github\.io(\.git)?|git@github\.com:Harry-Rogers/Harry-Rogers\.github\.io(\.git)?)$' &amp;&amp; git ls-remote origin main &gt; /dev/null</automated>
  </verify>
  <acceptance_criteria>
    - `git remote get-url origin` returns either `https://github.com/Harry-Rogers/Harry-Rogers.github.io.git` or `git@github.com:Harry-Rogers/Harry-Rogers.github.io.git`
    - `git ls-remote origin main` returns at least one line (remote `main` ref exists — confirms the stale repo is reachable)
    - `git status --porcelain` is empty
  </acceptance_criteria>
  <done>`origin` is configured and reachable; remote `main` ref is fetched but not merged.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner confirmation — destructive force-push to origin/main</name>
  <what-built>
    A local `main` branch containing the academicpages-based site (plans 01 and 02 commits), ready to overwrite the stale 2020 hand-rolled remote history on `Harry-Rogers/Harry-Rogers.github.io`.
  </what-built>
  <how-to-verify>
    Before the executor runs `git push --force origin main`, the owner must confirm:

    1. View the current remote history (will be permanently discarded):
       `git log origin/main --oneline | head -20`
    2. View what will replace it:
       `git log main --oneline`
    3. Confirm: this matches the locked CONTEXT.md decision "force-push, discard entirely. No `legacy-2020` tag/branch."
    4. If approved, the executor will run: `git push --force origin main`
    5. Then verify: `git ls-remote origin main` returns the SHA of local `HEAD` (i.e. `git rev-parse HEAD`).

    The force-push is irreversible without a third-party mirror of the old history. PROJECT.md and CONTEXT.md both confirm no salvage.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with `git push --force origin main`, or "abort" to halt and reconsider.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Execute the force-push (only after owner approval in Task 2)</name>
  <files>
    - (none — remote state change only)
  </files>
  <read_first>
    - .planning/phases/01-bootstrap-deploy/01-CONTEXT.md (locked decision 3 — force-push, no legacy tag/branch)
  </read_first>
  <action>
    Run exactly: `git push --force origin main`.

    Then verify: `git ls-remote origin main | awk '{print $1}'` outputs the same SHA as `git rev-parse HEAD`.

    Do NOT push tags. Do NOT push other branches. Do NOT enable any other branch protection at this stage.
  </action>
  <verify>
    <automated>[ "$(git ls-remote origin main | awk '{print $1}')" = "$(git rev-parse HEAD)" ]</automated>
  </verify>
  <acceptance_criteria>
    - `git ls-remote origin main | awk '{print $1}'` equals `git rev-parse HEAD` (i.e. remote `main` now points at local `HEAD`)
    - `git status --porcelain` is empty
  </acceptance_criteria>
  <done>Remote `origin/main` reflects the local academicpages-based site; the 2020 history is gone.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Owner sets GitHub Pages source = main branch / root</name>
  <what-built>
    Remote `main` now contains the academicpages-based site, but GitHub Pages may still be configured (from the legacy site era) with a different source — or with a custom GitHub Action — or even disabled. Setting source = `main` branch / `/` (root) tells GitHub to run the default Jekyll builder on every push.
  </what-built>
  <how-to-verify>
    Owner action (requires repo admin on `Harry-Rogers/Harry-Rogers.github.io`):

    1. Open `https://github.com/Harry-Rogers/Harry-Rogers.github.io/settings/pages` in a browser.
    2. Under "Build and deployment" → "Source", select **"Deploy from a branch"** (NOT "GitHub Actions").
    3. Under "Branch", select `main` and `/ (root)`. Click **Save**.
    4. Wait ~30-90 seconds. Reload `https://github.com/Harry-Rogers/Harry-Rogers.github.io/actions` and watch for a `pages-build-deployment` workflow run. It should turn green.
    5. Verify via CLI (if `gh` is authenticated):
       - `gh api repos/Harry-Rogers/Harry-Rogers.github.io/pages | jq -r .source.branch` should print `main`
       - `gh api repos/Harry-Rogers/Harry-Rogers.github.io/pages | jq -r .source.path` should print `/`
       - `gh run list -R Harry-Rogers/Harry-Rogers.github.io --workflow=pages-build-deployment --limit 1 --json conclusion --jq '.[0].conclusion'` should print `success`

    If the latest `pages-build-deployment` shows `failure`, click into the run, read the Jekyll build log, and report the error in the resume-signal so plan 02 can be revisited.
  </how-to-verify>
  <resume-signal>Type "pages source set, build green" once Settings → Pages shows source=main/root AND the latest pages-build-deployment is green. Type "build failed: [paste error]" if the Jekyll build errors out.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Verify canonical URL serves HTTP 200 with no academicpages references</name>
  <files>
    - (none — read-only HTTP verification)
  </files>
  <read_first>
    - .planning/ROADMAP.md (Phase 1 Success Criteria #1 and #3 — canonical URL at root, no subpath, no academicpages references in view-source)
  </read_first>
  <action>
    Run the following verification commands in order. All must succeed:

    1. `curl -sI https://harry-rogers.github.io/ | head -1` — expect `HTTP/2 200` (or `HTTP/1.1 200 OK` depending on negotiation).
    2. `curl -s https://harry-rogers.github.io/ | grep -iE '<link[^>]+rel=["'"'"']?canonical' | head -1` — expect the canonical URL to contain `harry-rogers.github.io` and NOT contain `academicpages`.
    3. `curl -s https://harry-rogers.github.io/ | grep -ciE 'academicpages\.github\.io'` — expect `0` (no academicpages URLs leaking through).
    4. `curl -sI https://harry-rogers.github.io/sitemap.xml | head -1` — expect `HTTP/2 200` (confirms `jekyll-sitemap` plugin ran).
    5. `curl -sI https://harry-rogers.github.io/feed.xml | head -1` — expect `HTTP/2 200` (confirms `jekyll-feed` plugin ran).
    6. `gh api repos/Harry-Rogers/Harry-Rogers.github.io/pages 2>/dev/null | jq -r .source.branch` — expect `main`. (If `gh` not authenticated for this repo, owner-verifies via the UI step in Task 4 instead.)

    If any check fails:
    - 404 on `/` → Pages source not yet set correctly (revisit Task 4)
    - 200 on `/` but `academicpages` URLs in source → `_config.yml` url/baseurl regression (revisit plan 02)
    - 404 on `/sitemap.xml` or `/feed.xml` → plugin not active in `_config.yml` (revisit plan 02)

    DNS/CDN caching: if 404 right after Task 4 went green, wait 60s and retry. After 5 minutes of failure, treat as a real defect.
  </action>
  <verify>
    <automated>curl -sI https://harry-rogers.github.io/ | head -1 | grep -qE 'HTTP/[12](\.[01])? 200' &amp;&amp; ! curl -s https://harry-rogers.github.io/ | grep -qiE 'academicpages\.github\.io' &amp;&amp; curl -sI https://harry-rogers.github.io/sitemap.xml | head -1 | grep -qE 'HTTP/[12](\.[01])? 200' &amp;&amp; curl -sI https://harry-rogers.github.io/feed.xml | head -1 | grep -qE 'HTTP/[12](\.[01])? 200'</automated>
  </verify>
  <acceptance_criteria>
    - `curl -sI https://harry-rogers.github.io/ | head -1` matches `HTTP/[12](\.[01])? 200`
    - `curl -s https://harry-rogers.github.io/ | grep -ciE 'academicpages\.github\.io'` returns `0`
    - `curl -sI https://harry-rogers.github.io/sitemap.xml | head -1` matches `HTTP/[12](\.[01])? 200`
    - `curl -sI https://harry-rogers.github.io/feed.xml | head -1` matches `HTTP/[12](\.[01])? 200`
    - `gh api repos/Harry-Rogers/Harry-Rogers.github.io/pages | jq -r .source.branch` returns `main` (or the owner has visually confirmed via Settings → Pages)
    - `gh run list -R Harry-Rogers/Harry-Rogers.github.io --workflow=pages-build-deployment --limit 1 --json conclusion --jq '.[0].conclusion'` returns `success` (or owner has visually confirmed)
  </acceptance_criteria>
  <done>Canonical URL serves an academicpages-based Jekyll site with no `academicpages.github.io` references in the rendered HTML, `sitemap.xml` and `feed.xml` are reachable, and Pages source is confirmed as `main`/root with the latest build green. Gates G1 and G3 satisfied.</done>
</task>

</tasks>

<verification>
At plan exit, Bootstrap Gates G1 (canonical URL at root, no subpath), G2 (verified live via view-source — no academicpages references), G3 (main branch source, last build green), G4 (plugins active and constrained to allowlist — sitemap.xml and feed.xml served, no jekyll-scholar errors), and G5 (Gemfile.lock + .ruby-version were verified at source in plan 02) are all green.

The remaining gate to verify is the Codespaces UAT (DEPLOY-05) — owned by plan 05.
</verification>

<success_criteria>
- All five tasks pass their acceptance criteria.
- Owner has confirmed the force-push and Pages source configuration.
- The site at `https://harry-rogers.github.io/` returns HTTP 200 with no subpath and no academicpages references.
</success_criteria>

<output>
Create `.planning/phases/01-bootstrap-deploy/01-04-SUMMARY.md` when done.
</output>
