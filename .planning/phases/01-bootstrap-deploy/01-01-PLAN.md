---
phase: 01-bootstrap-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "(many — academicpages template files copied into working tree; preserves .planning/, CLAUDE.md, assets/images/profile-original.jpg, .git/)"
  - _config.yml
  - Gemfile
  - .gitignore
  - _publications/
  - _talks/
  - _portfolio/
  - _teaching/
  - _posts/
  - _pages/
  - _layouts/
  - _includes/
  - _sass/
  - _data/
  - assets/
  - .devcontainer/
autonomous: true
requirements: [DEPLOY-01]
must_haves:
  truths:
    - "Academicpages template files exist in the working tree"
    - "Local preserved artifacts (.planning/, CLAUDE.md, assets/images/profile-original.jpg) are unchanged"
    - "Academicpages is NOT configured as a git remote"
    - "The import is recorded as a single atomic commit (or a small set of atomic commits) on main"
  artifacts:
    - path: _config.yml
      provides: "Jekyll site configuration imported from academicpages (will be customised in plan 02)"
    - path: Gemfile
      provides: "Bundler manifest from academicpages (will be reviewed in plan 02)"
    - path: .devcontainer/devcontainer.json
      provides: "Codespaces dev container shipped unmodified from academicpages (needed for DEPLOY-05)"
    - path: _publications/
      provides: "Imported demo collection (purged in Phase 2)"
    - path: _talks/
      provides: "Imported demo collection (purged in Phase 2)"
  key_links:
    - from: "working tree"
      to: "academicpages template files"
      via: "rsync copy from /tmp/academicpages-src/"
      pattern: "ls _config.yml _layouts _includes _sass"
---

<objective>
Materialise the academicpages template into this working tree without clobbering local artifacts (`.planning/`, `CLAUDE.md`, `assets/images/profile-original.jpg`), and commit as an atomic import on `main`. This is the foundation for every subsequent plan; without it there is no Jekyll site to configure, push, or verify.

Purpose: Per CONTEXT.md decision "Bootstrap mechanism = clone academicpages to /tmp, copy tracked files into working tree EXCLUDING preserved paths, single (or a few) atomic commits, do NOT add academicpages as a git remote." Demo content is imported as-is — Phase 2 owns the purge.

Output: A working tree containing the full academicpages template plus the existing preserved files, recorded as a clean atomic commit (or small set of atomic commits) on `main`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-bootstrap-deploy/01-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Clone academicpages to a temp dir and import its tracked files into the working tree</name>
  <files>
    - many (all academicpages tracked files except those listed in the rsync exclude set below)
  </files>
  <read_first>
    - .planning/phases/01-bootstrap-deploy/01-CONTEXT.md (decision: "clone academicpages to /tmp/academicpages-src/, copy tracked files into the working tree EXCLUDING `.planning/`, `CLAUDE.md`, `assets/images/profile-original.jpg`, and `.git/`")
    - CLAUDE.md ("What NOT to Use" table — no `remote_theme`; academicpages already vendors Minimal Mistakes)
  </read_first>
  <action>
    Execute the following exact sequence from the repo root (`/home/harry/Harry-Rogers.github.io/`):

    1. `rm -rf /tmp/academicpages-src` (idempotent reset)
    2. `git clone --depth 1 https://github.com/academicpages/academicpages.github.io.git /tmp/academicpages-src`
    3. From `/tmp/academicpages-src/`, list tracked files only (do NOT copy `.git/`): use `git -C /tmp/academicpages-src ls-files` to enumerate.
    4. Copy every tracked file into the current working tree using `rsync -a` with these EXACT excludes (so we never overwrite preserved local artifacts):
       `rsync -a --files-from=<(git -C /tmp/academicpages-src ls-files) --exclude='.git/' --exclude='.planning/' --exclude='CLAUDE.md' --exclude='assets/images/profile-original.jpg' /tmp/academicpages-src/ ./`
       (Note: `--files-from` only lists tracked files; the additional `--exclude` lines are belt-and-braces guards in case any of those paths slip through.)
    5. Verify the preserved files are untouched:
       - `test -f .planning/PROJECT.md` (must exist)
       - `test -f CLAUDE.md` (must exist)
       - `test -f assets/images/profile-original.jpg` (must exist)
    6. Verify academicpages is NOT a configured git remote: `git remote -v` must NOT mention `academicpages`.
    7. Stage all changes: `git add -A`.
    8. Commit atomically: `git commit -m "feat(bootstrap): import academicpages template"`.
       (If natural seams emerge, planner permits splitting into 2-3 atomic commits — e.g. one for `_layouts/_includes/_sass`, one for `_pages/_data/_publications/_talks/_portfolio/_teaching/_posts`, one for top-level `_config.yml/Gemfile/.devcontainer/.gitignore/assets/`. A single commit is also acceptable.)
    9. Clean up: `rm -rf /tmp/academicpages-src`.

    Do NOT add academicpages as a git remote. Do NOT use `--allow-unrelated-histories`. Do NOT edit `_config.yml` here — plan 02 owns that.
  </action>
  <verify>
    <automated>test -f _config.yml &amp;&amp; test -f Gemfile &amp;&amp; test -f .devcontainer/devcontainer.json &amp;&amp; test -d _layouts &amp;&amp; test -d _includes &amp;&amp; test -d _sass &amp;&amp; test -d _publications &amp;&amp; test -d _talks &amp;&amp; test -d _portfolio &amp;&amp; test -d _teaching &amp;&amp; test -f .planning/PROJECT.md &amp;&amp; test -f CLAUDE.md &amp;&amp; test -f assets/images/profile-original.jpg &amp;&amp; ! git remote -v | grep -q academicpages</automated>
  </verify>
  <acceptance_criteria>
    - `test -f _config.yml` exits 0
    - `test -f Gemfile` exits 0
    - `test -f .devcontainer/devcontainer.json` exits 0
    - `test -d _layouts && test -d _includes && test -d _sass` exits 0
    - `test -d _publications && test -d _talks && test -d _portfolio && test -d _teaching` exits 0
    - `test -f .planning/PROJECT.md` exits 0 (preserved)
    - `test -f CLAUDE.md` exits 0 (preserved)
    - `test -f assets/images/profile-original.jpg` exits 0 (preserved)
    - `git remote -v | grep -c academicpages` returns `0`
    - `git log -1 --pretty=%s` matches the regex `^feat\(bootstrap\): import academicpages template`
    - `git status --porcelain` returns empty (everything committed)
  </acceptance_criteria>
  <done>The academicpages template is materialised in the working tree, preserved files are intact, and the import is committed atomically on `main`.</done>
</task>

</tasks>

<verification>
After this plan completes, the next plans (02 site config, 03 doc reconciliation) can read `_config.yml`, `Gemfile`, and `.ruby-version`-equivalent files from the working tree. The template is present but not yet customised — `_config.yml` will still say `url: "https://academicpages.github.io"` and similar, which plan 02 fixes.
</verification>

<success_criteria>
- All acceptance criteria above pass.
- A subsequent `bundle exec jekyll build` (run in plan 02 with Ruby available) will have the source tree it needs.
</success_criteria>

<output>
Create `.planning/phases/01-bootstrap-deploy/01-01-SUMMARY.md` when done.
</output>
