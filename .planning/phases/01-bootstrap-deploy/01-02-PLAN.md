---
phase: 01-bootstrap-deploy
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - _config.yml
  - .ruby-version
  - Gemfile
  - Gemfile.lock
autonomous: false
requirements: [DEPLOY-02, DEPLOY-04]
must_haves:
  truths:
    - "_config.yml declares the canonical URL with no project subpath and no academicpages references"
    - "_config.yml's plugins list contains exactly the four allowlisted plugins (no jekyll-scholar, no jekyll-gist, no jekyll-paginate, no jemoji)"
    - ".ruby-version pins exactly 3.3.4"
    - "Gemfile uses the github-pages meta-gem (no hand-pinned jekyll version)"
    - "Gemfile.lock is generated and committed"
  artifacts:
    - path: _config.yml
      provides: "Site config with canonical url, empty baseurl, correct repository, and allowlist-only plugins"
      contains: 'url: "https://harry-rogers.github.io"'
    - path: .ruby-version
      provides: "Pins local + Codespaces Ruby to the version pages-gem uses"
      contains: "3.3.4"
    - path: Gemfile
      provides: "Bundler manifest using the github-pages meta-gem"
      contains: 'gem "github-pages"'
    - path: Gemfile.lock
      provides: "Locked dependency graph for reproducible local builds"
  key_links:
    - from: "_config.yml plugins:"
      to: "github-pages allowlist at pages.github.com/versions/"
      via: "strict subset"
      pattern: "^\\s*-\\s*jekyll-(feed|sitemap|seo-tag|redirect-from)\\s*$"
---

<objective>
Customise the imported academicpages config so the site builds at the canonical URL `https://harry-rogers.github.io/` with no project subpath, pin Ruby to 3.3.4, switch the Gemfile to the `github-pages` meta-gem, and commit a `Gemfile.lock` for reproducible local/Codespaces builds. This satisfies Bootstrap Gates G2 (`baseurl: ""`, `url: "https://harry-rogers.github.io"`), G4 (allowlist-only plugins), and G5 (`Gemfile.lock` committed, `.ruby-version` = `3.3.4`).

Purpose: Per CONTEXT.md and CLAUDE.md tech-stack research, the four plugins enabled are `jekyll-feed`, `jekyll-sitemap`, `jekyll-seo-tag`, `jekyll-redirect-from`. Anything else (jekyll-scholar, jekyll-gist, jekyll-paginate, jemoji, jekyll-mentions, jekyll-avatar) is dropped. `Gemfile.lock` is generated via the first available runtime: a local Ruby 3.3.x install (rbenv/asdf/system), or a Ruby 3.3.4 Docker/Podman container, or — if neither is available — an explicit Escalation Gate that asks the owner to produce the lockfile from any Ruby 3.3.4 environment they have access to.

Output: `_config.yml`, `.ruby-version`, `Gemfile`, and `Gemfile.lock` all match the locked spec, committed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-bootstrap-deploy/01-CONTEXT.md
@CLAUDE.md
@_config.yml
@Gemfile
</context>

<tasks>

<task type="auto">
  <name>Task 1: Edit _config.yml — set url, baseurl, repository, and plugin allowlist</name>
  <files>
    - _config.yml
  </files>
  <read_first>
    - _config.yml (the file being modified — read in full to locate the existing url/baseurl/repository/plugins keys imported from academicpages)
    - .planning/phases/01-bootstrap-deploy/01-CONTEXT.md (Plugin allowlist decision; exact target values for url/baseurl/repository)
    - CLAUDE.md ("Whitelisted Plugins" table; "What NOT to Use" table — confirms `jekyll-gist`/`jemoji`/`jekyll-paginate` are dropped)
  </read_first>
  <action>
    Edit `_config.yml` so the following exact key/value pairs are present (replacing whatever academicpages shipped):

    1. `url: "https://harry-rogers.github.io"` (exact double-quoted string; no trailing slash)
    2. `baseurl: ""` (exact empty double-quoted string — NOT `"/"`, NOT `"/academicpages.github.io"`, NOT unset)
    3. `repository: "Harry-Rogers/Harry-Rogers.github.io"` (exact case)
    4. Top-level `plugins:` block contains EXACTLY these four entries, in this order, and no others:
       ```
       plugins:
         - jekyll-feed
         - jekyll-sitemap
         - jekyll-seo-tag
         - jekyll-redirect-from
       ```
       Remove `jekyll-gist`, `jekyll-paginate`, `jemoji`, `jekyll-mentions`, `jekyll-avatar`, `jekyll-include-cache`, and any other plugin entries that may have shipped from academicpages. `jekyll-seo-tag` must be ADDED if academicpages did not ship it.
    5. If a `whitelist:` block exists (some older academicpages forks duplicate the plugin list under `whitelist:`), apply the same exact four-entry list there. If it does not exist, do not add it.

    Do NOT change `minimal_mistakes_skin`, `author`, `defaults`, or any other keys — Phase 3 owns theme work and Phase 2 owns author content.

    Confirm via grep after edit:
    - `grep -n '^url: "https://harry-rogers.github.io"$' _config.yml` returns exactly one line
    - `grep -n '^baseurl: ""$' _config.yml` returns exactly one line
    - `grep -n '^repository: "Harry-Rogers/Harry-Rogers.github.io"$' _config.yml` returns exactly one line
    - `grep -nE '^\s*-\s*jekyll-(scholar|admin|paginate|paginate-v2|gist|mentions|avatar)\b' _config.yml` returns no lines
    - `grep -nE '^\s*-\s*jemoji\s*$' _config.yml` returns no lines
  </action>
  <verify>
    <automated>grep -qx 'url: "https://harry-rogers.github.io"' _config.yml &amp;&amp; grep -qx 'baseurl: ""' _config.yml &amp;&amp; grep -qx 'repository: "Harry-Rogers/Harry-Rogers.github.io"' _config.yml &amp;&amp; grep -qE '^\s*-\s*jekyll-feed\s*$' _config.yml &amp;&amp; grep -qE '^\s*-\s*jekyll-sitemap\s*$' _config.yml &amp;&amp; grep -qE '^\s*-\s*jekyll-seo-tag\s*$' _config.yml &amp;&amp; grep -qE '^\s*-\s*jekyll-redirect-from\s*$' _config.yml &amp;&amp; ! grep -qE '^\s*-\s*(jekyll-scholar|jekyll-admin|jekyll-paginate|jekyll-paginate-v2|jekyll-gist|jekyll-mentions|jekyll-avatar|jemoji)\b' _config.yml</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^url: "https://harry-rogers.github.io"$' _config.yml` returns `1`
    - `grep -c '^baseurl: ""$' _config.yml` returns `1`
    - `grep -c '^repository: "Harry-Rogers/Harry-Rogers.github.io"$' _config.yml` returns `1`
    - `grep -cE '^\s*-\s*jekyll-feed\s*$' _config.yml` returns `1` (or `2` if `whitelist:` block also exists)
    - `grep -cE '^\s*-\s*jekyll-sitemap\s*$' _config.yml` >= `1`
    - `grep -cE '^\s*-\s*jekyll-seo-tag\s*$' _config.yml` >= `1`
    - `grep -cE '^\s*-\s*jekyll-redirect-from\s*$' _config.yml` >= `1`
    - `grep -cE '^\s*-\s*(jekyll-scholar|jekyll-admin|jekyll-paginate|jekyll-paginate-v2|jekyll-gist|jekyll-mentions|jekyll-avatar|jemoji)\b' _config.yml` returns `0`
  </acceptance_criteria>
  <done>`_config.yml` declares the canonical URL and plugin allowlist exactly as specified; no forbidden plugins remain.</done>
</task>

<task type="auto">
  <name>Task 2: Pin .ruby-version, switch Gemfile to github-pages meta-gem, and generate Gemfile.lock (with runtime detection)</name>
  <files>
    - .ruby-version
    - Gemfile
    - Gemfile.lock
  </files>
  <read_first>
    - Gemfile (the file being modified — read in full to see what academicpages shipped; academicpages typically already references `github-pages` but verify)
    - CLAUDE.md ("Recommended Stack" table — `ruby@3.3.4`, `github-pages` meta-gem, `Gemfile.lock` is committed; "Installation" section describes `bundle install` flow)
    - .planning/phases/01-bootstrap-deploy/01-CONTEXT.md (locked decisions 7, 8, 9 — `.ruby-version` exactly `3.3.4`, Gemfile uses `github-pages`, lockfile committed)
  </read_first>
  <action>
    Step A — Pin Ruby:
    1. Write `.ruby-version` containing exactly the string `3.3.4\n` (one line, trailing newline, no extra whitespace).

    Step B — Ensure Gemfile uses the `github-pages` meta-gem:
    2. Open `Gemfile`. It MUST contain a line equivalent to `gem "github-pages", group: :jekyll_plugins` (academicpages typically ships this; if it ships hand-pinned `gem "jekyll"`, REPLACE that line with `gem "github-pages", group: :jekyll_plugins`).
    3. The Gemfile MUST NOT contain `gem "jekyll-scholar"` (and must not contain any non-allowlisted plugin gem). If present, remove.
    4. Each of the four allowlisted plugins is ALREADY provided transitively by `github-pages`; no need to list them explicitly in the Gemfile. Remove any explicit `gem "jekyll-feed"` / `gem "jekyll-sitemap"` / `gem "jekyll-seo-tag"` / `gem "jekyll-redirect-from"` lines if they would conflict with the meta-gem — academicpages usually does not include them, but verify.

    Step C — Generate `Gemfile.lock` using the FIRST available runtime, in this priority order. Try each in turn; on the first success, skip the rest of Step C and proceed to Step D.

       C.1 — Local Ruby 3.3.x (preferred — fastest, no container overhead):
         - Detect: `command -v ruby >/dev/null && ruby -v 2>/dev/null | grep -qE 'ruby 3\.3\.[0-9]+'`.
         - If present, run: `gem install bundler --no-document 2>/dev/null || true; bundle install`.
         - Note: matching Ruby 3.3.x (any patch) is acceptable for lockfile generation against the `github-pages` meta-gem; the `.ruby-version` file still pins `3.3.4` so production parity holds.
         - On success, proceed to Step D.

       C.2 — Docker:
         - Detect: `command -v docker >/dev/null && docker info >/dev/null 2>&1`.
         - If available, run: `docker run --rm -v "$PWD":/app -w /app ruby:3.3.4 bash -lc "gem install bundler --no-document && bundle install"`.
         - On success, proceed to Step D.

       C.3 — Podman:
         - Detect: `command -v podman >/dev/null && podman info >/dev/null 2>&1`.
         - If available, run: `podman run --rm -v "$PWD":/app -w /app ruby:3.3.4 bash -lc "gem install bundler --no-document && bundle install"`.
         - On success, proceed to Step D.

       C.4 — None of the above available → STOP and surface the Escalation Gate (Task 3 below). Do NOT commit; do NOT proceed to Step D. The executor halts here and the next task takes over.

    Step D — Commit (only reached if Step C succeeded):
    5. Confirm `Gemfile.lock` exists and is non-empty (`test -s Gemfile.lock`).
    6. Confirm the lockfile resolved the meta-gem: `grep -q 'github-pages' Gemfile.lock`.
    7. Stage: `git add .ruby-version Gemfile Gemfile.lock _config.yml`.
    8. Commit: `git commit -m "feat(bootstrap): pin ruby 3.3.4, github-pages gem, canonical URL, plugin allowlist"`.
  </action>
  <verify>
    <automated>[ "$(cat .ruby-version)" = "3.3.4" ] &amp;&amp; grep -qE '^gem\s+["'"'"']github-pages["'"'"']' Gemfile &amp;&amp; ! grep -qE '^gem\s+["'"'"']jekyll-scholar["'"'"']' Gemfile &amp;&amp; { test -s Gemfile.lock || { echo "Gemfile.lock missing — Escalation Gate (Task 3) must run"; exit 1; }; } &amp;&amp; git ls-files --error-unmatch Gemfile.lock &amp;&amp; git ls-files --error-unmatch .ruby-version</automated>
  </verify>
  <acceptance_criteria>
    - `cat .ruby-version` outputs exactly `3.3.4` (single line, trailing newline)
    - `wc -l < .ruby-version` returns `1`
    - `grep -cE '^gem\s+["'"'"']github-pages["'"'"']' Gemfile` returns `1`
    - `grep -cE '^gem\s+["'"'"']jekyll-scholar["'"'"']' Gemfile` returns `0`
    - If Step C succeeded autonomously: `test -s Gemfile.lock` exits 0, `git ls-files Gemfile.lock` outputs `Gemfile.lock`, `grep -c "github-pages" Gemfile.lock` >= `1`, `git log -1 --pretty=%s` matches `^feat\(bootstrap\): pin ruby 3.3.4`.
    - If Step C reached C.4 (no runtime available): this task halts and Task 3 (Escalation Gate) takes over. Acceptance for Gemfile.lock-related criteria moves to Task 3's `<done>`.
  </acceptance_criteria>
  <done>`.ruby-version` and `Gemfile` are correct. `Gemfile.lock` is either generated and committed in this task (autonomous path C.1/C.2/C.3) OR control has passed to Task 3 (Escalation Gate) which will complete the commit after owner action.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Escalation Gate — owner generates Gemfile.lock from any Ruby 3.3.4 environment (only runs if Task 2 hit C.4)</name>
  <skip-if>Task 2 already committed `Gemfile.lock` (i.e. one of C.1/C.2/C.3 succeeded). In that case, this task is a no-op — mark it complete and move on.</skip-if>
  <what-built>
    Task 2 completed `.ruby-version` and `Gemfile` edits but could not generate `Gemfile.lock` autonomously: no local Ruby 3.3.x, no Docker, no Podman detected on this executor. Everything else is staged-ready; only the lockfile is missing before commit.
  </what-built>
  <how-to-verify>
    Owner action — generate `Gemfile.lock` from any Ruby 3.3.4 environment you have access to. Pick whichever is easiest:

    Option 1 — Local rbenv/asdf/chruby on your own machine:
      1. `rbenv install 3.3.4` (or `asdf install ruby 3.3.4`).
      2. `cd` into this repo (which already has `.ruby-version` = 3.3.4).
      3. `gem install bundler && bundle install`.
      4. `git add Gemfile.lock && git commit -m "feat(bootstrap): pin ruby 3.3.4, github-pages gem, canonical URL, plugin allowlist"`.

    Option 2 — A GitHub Codespace opened ahead of Plan 05:
      1. Open this repo in a Codespace (`Code → Codespaces → Create codespace on main`).
      2. The shipped devcontainer (academicpages-provided) already has Ruby 3.3.4.
      3. In the Codespace terminal: `bundle install`.
      4. Commit `Gemfile.lock` from the Codespace and push.

    Option 3 — A colleague's machine or any one-off Ruby 3.3.4 environment:
      1. Copy `Gemfile` and `.ruby-version` to that machine.
      2. `bundle install` to produce `Gemfile.lock`.
      3. Paste the resulting `Gemfile.lock` back into this repo's root.
      4. `git add Gemfile.lock _config.yml .ruby-version Gemfile && git commit -m "feat(bootstrap): pin ruby 3.3.4, github-pages gem, canonical URL, plugin allowlist"`.

    Then verify locally:
      - `test -s Gemfile.lock` exits 0
      - `grep -q 'github-pages' Gemfile.lock` exits 0
      - `git ls-files --error-unmatch Gemfile.lock` exits 0
      - `git log -1 --pretty=%s` matches `^feat\(bootstrap\): pin ruby 3.3.4`
  </how-to-verify>
  <resume-signal>Reply "lockfile committed" once `Gemfile.lock` is generated, committed (single atomic commit with the staged `_config.yml`, `.ruby-version`, `Gemfile`), and the four verification checks above all pass. If you hit a problem (e.g. bundle resolution error), paste the error and we'll fix the Gemfile.</resume-signal>
  <done>`Gemfile.lock` is committed alongside `.ruby-version`, `Gemfile`, and `_config.yml` in a single `feat(bootstrap):` commit; all autonomous verification checks now pass.</done>
</task>

</tasks>

<verification>
After this plan completes, all of Bootstrap Gates G2, G4, and G5 are satisfied at the source level. G1 (canonical URL serves at root) and G3 (Pages source = main, last build green) are verified in plan 04 after force-push.
</verification>

<success_criteria>
- All acceptance criteria above pass.
- A `bundle exec jekyll build` inside a Ruby 3.3.4 environment (Codespace devcontainer, container, or local) produces `_site/` with no errors. (This is exercised in plan 05 UAT; not blocking here.)
</success_criteria>

<output>
Create `.planning/phases/01-bootstrap-deploy/01-02-SUMMARY.md` when done.
</output>
