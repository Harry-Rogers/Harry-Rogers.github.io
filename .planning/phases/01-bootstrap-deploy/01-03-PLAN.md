---
phase: 01-bootstrap-deploy
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
autonomous: true
requirements: [DEPLOY-04]
must_haves:
  truths:
    - "REQUIREMENTS.md DEPLOY-04 wording lists exactly the four locked plugins (jekyll-feed, jekyll-sitemap, jekyll-seo-tag, jekyll-redirect-from) and no others"
    - "REQUIREMENTS.md no longer references jekyll-gist, jekyll-paginate, or jemoji in the DEPLOY-04 bullet"
    - "The doc update is committed as a separate `docs(req):` commit (not bundled with code changes)"
  artifacts:
    - path: .planning/REQUIREMENTS.md
      provides: "Canonical spec aligned with CONTEXT.md plugin allowlist decision"
      contains: "jekyll-seo-tag"
  key_links:
    - from: ".planning/REQUIREMENTS.md DEPLOY-04"
      to: "_config.yml plugins:"
      via: "explicit cross-reference to the same four plugins"
      pattern: "jekyll-(feed|sitemap|seo-tag|redirect-from)"
---

<objective>
Reconcile the stale wording of REQUIREMENTS.md DEPLOY-04 with the locked plugin allowlist from CONTEXT.md and CLAUDE.md so the spec stays canonical going forward. The current DEPLOY-04 line lists `jekyll-paginate`, `jekyll-gist`, `jemoji` (now dropped) and omits `jekyll-seo-tag` (now adopted).

Purpose: Per CONTEXT.md decision: "REQUIREMENTS.md DEPLOY-04 lists `jekyll-gist` and `jemoji` and omits `jekyll-seo-tag` — this is stale wording from before the tech-stack research. As part of Phase 1, update DEPLOY-04 to match this decision so REQUIREMENTS.md stays the canonical spec going forward. Commit that doc update separately from code changes."

Output: A single `docs(req):` commit updating only the DEPLOY-04 bullet in `.planning/REQUIREMENTS.md`.

This plan is independent of the template import (plan 01) — it edits a `.planning/` file that plan 01 explicitly preserves — and so runs in Wave 1 in parallel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-bootstrap-deploy/01-CONTEXT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update REQUIREMENTS.md DEPLOY-04 wording to match the locked plugin allowlist</name>
  <files>
    - .planning/REQUIREMENTS.md
  </files>
  <read_first>
    - .planning/REQUIREMENTS.md (the file being modified — locate the DEPLOY-04 line, currently around line 14)
    - .planning/phases/01-bootstrap-deploy/01-CONTEXT.md ("Plugin allowlist" decision — the four locked plugins and the explicit doc-reconciliation instruction)
    - CLAUDE.md ("Whitelisted Plugins" table — confirms `jekyll-seo-tag` IN, `jekyll-gist`/`jemoji`/`jekyll-paginate` OUT for v1)
  </read_first>
  <action>
    Replace the existing DEPLOY-04 line in `.planning/REQUIREMENTS.md` with this exact wording (single line, no parenthetical naming dropped plugins — CLAUDE.md already documents the rationale for what is excluded):

    ```
    - [ ] **DEPLOY-04**: `.ruby-version` pinned to `3.3.4` and `Gemfile.lock` committed; `_config.yml` `plugins:` is exactly the four GitHub Pages allowlist plugins this site uses: `jekyll-feed`, `jekyll-sitemap`, `jekyll-seo-tag`, `jekyll-redirect-from`. No other plugins are enabled in v1.
    ```

    Do NOT change any other line in REQUIREMENTS.md. Do NOT touch the Traceability table (DEPLOY-04 still maps to Phase 1, status still Pending). Do NOT name the dropped plugins (`jekyll-gist`, `jekyll-paginate`, `jemoji`) anywhere on the DEPLOY-04 line — CLAUDE.md's "What NOT to Use" and "Whitelisted Plugins" tables are the canonical source for that detail.

    Then:
    1. Update the "Last updated" footer line at the bottom of REQUIREMENTS.md from `*Last updated: 2026-05-17 after roadmap creation*` to `*Last updated: 2026-05-17 after Phase 1 plugin-allowlist reconciliation*`.
    2. Stage only this file: `git add .planning/REQUIREMENTS.md`.
    3. Commit: `git commit -m "docs(req): align DEPLOY-04 plugin list with locked allowlist"`.
  </action>
  <verify>
    <automated>grep -qE '\*\*DEPLOY-04\*\*.*jekyll-seo-tag' .planning/REQUIREMENTS.md &amp;&amp; ! grep -E '\*\*DEPLOY-04\*\*' .planning/REQUIREMENTS.md | grep -qE '(jekyll-gist|jemoji|jekyll-paginate)' &amp;&amp; grep -qE '\*\*DEPLOY-04\*\*.*jekyll-feed.*jekyll-sitemap.*jekyll-seo-tag.*jekyll-redirect-from' .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '\*\*DEPLOY-04\*\*' .planning/REQUIREMENTS.md` returns `1` (still exactly one DEPLOY-04 line, in the requirements section — the Traceability row uses `| DEPLOY-04 |` and is unaffected)
    - `grep -E '\*\*DEPLOY-04\*\*' .planning/REQUIREMENTS.md | grep -c 'jekyll-seo-tag'` returns `1`
    - `grep -E '\*\*DEPLOY-04\*\*' .planning/REQUIREMENTS.md | grep -cE '(jekyll-gist|jemoji|jekyll-paginate)\b'` returns `0` (none of the dropped plugins appear in the requirement line itself)
    - `grep -E '\*\*DEPLOY-04\*\*' .planning/REQUIREMENTS.md | grep -cE 'jekyll-feed.*jekyll-sitemap.*jekyll-seo-tag.*jekyll-redirect-from'` returns `1` (all four plugins listed in the locked order)
    - `git log -1 --pretty=%s` matches `^docs\(req\): align DEPLOY-04`
    - `git diff HEAD~1 HEAD --stat` shows exactly one file changed (`.planning/REQUIREMENTS.md`)
  </acceptance_criteria>
  <done>REQUIREMENTS.md DEPLOY-04 wording matches the locked plugin allowlist; the change is committed as a standalone `docs(req):` commit touching only that one file.</done>
</task>

</tasks>

<verification>
After this plan completes, the spec aligns with both CLAUDE.md and CONTEXT.md, and the only source of plugin truth for downstream phases is consistent across REQUIREMENTS.md, CLAUDE.md, and `_config.yml` (the last being delivered by plan 02).
</verification>

<success_criteria>
All acceptance criteria above pass.
</success_criteria>

<output>
Create `.planning/phases/01-bootstrap-deploy/01-03-SUMMARY.md` when done.
</output>
