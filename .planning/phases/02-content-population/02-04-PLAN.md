---
phase: 02-content-population
plan: 04
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - _publications/2020-04-01-uk-ras-seeding-agri-robot.md
  - _publications/2023-04-01-taros-precision-spraying-evaluation.md
  - _publications/2023-08-01-ieee-case-deposit-identification.md
  - _publications/2023-11-01-kdir-interpretable-quantized-cnn.md
  - _publications/2024-02-01-ncaa-domain-specific-augmentations.md
  - _publications/2024-09-01-epia-clock-drawing-test.md
  - _publications/2024-10-01-arxiv-post-spraying-evaluation.md
  - _publications/2025-06-08-ieee-iscc-spirometry-qa.md
  - _includes/bibtex-block.html
  - _pages/publications.html
autonomous: true
requirements:
  - PUBS-01
  - PUBS-02
  - PUBS-03
  - PUBS-04
  - PUBS-05
must_haves:
  truths:
    - "All 8 publications exist as Markdown files in _publications/"
    - "Each publication front matter has: title, authors (Harry bolded), venue, year, link, abstract, bibtex"
    - "KDIR 2023 entry is visibly tagged 'Best Paper Award'"
    - "/publications/ page lists entries in reverse-chronological order"
    - "Each publication detail page renders a copyable BibTeX block"
  artifacts:
    - path: "_publications/"
      provides: "8 hand-authored publication markdown files"
    - path: "_includes/bibtex-block.html"
      provides: "Liquid include that renders front-matter bibtex as a <pre><code> block"
  key_links:
    - from: "_publications/2023-11-01-kdir-interpretable-quantized-cnn.md"
      to: "Best Paper Award badge"
      via: "front matter or visible body marker"
      pattern: "Best Paper"
    - from: "individual publication layout"
      to: "_includes/bibtex-block.html"
      via: "Liquid include"
      pattern: "include bibtex-block"
---

<objective>
Author all 8 publications as Markdown files in `_publications/`, each with full front matter (title, authors with `**Rogers, H.**` bolded, venue, year, DOI/arXiv link, citation count, abstract, hand-authored BibTeX). Create a `bibtex-block.html` include so each publication page renders its BibTeX in a copyable `<pre>` block. KDIR 2023 carries a "Best Paper Award" badge.

Purpose: PUBS-01..05 — the publications surface is the single most important credibility signal on an academic site.
Output: 8 publication MD files; 1 Liquid include; (optionally) light edit to publications listing layout.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@CLAUDE.md
@.planning/REQUIREMENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 8 publication Markdown files in _publications/</name>
  <read_first>
    - .planning/PROJECT.md (Published Work section — all 8 papers with venues, years, citation counts)
    - CLAUDE.md (Liquid / Data File Conventions; BibTeX section — front matter authoring, no jekyll-scholar)
    - .planning/REQUIREMENTS.md (PUBS-02 — required front matter fields; PUBS-04 — all 8 enumerated; PUBS-05 — KDIR Best Paper badge)
  </read_first>
  <files>
    _publications/2020-04-01-uk-ras-seeding-agri-robot.md,
    _publications/2023-04-01-taros-precision-spraying-evaluation.md,
    _publications/2023-08-01-ieee-case-deposit-identification.md,
    _publications/2023-11-01-kdir-interpretable-quantized-cnn.md,
    _publications/2024-02-01-ncaa-domain-specific-augmentations.md,
    _publications/2024-09-01-epia-clock-drawing-test.md,
    _publications/2024-10-01-arxiv-post-spraying-evaluation.md,
    _publications/2025-06-08-ieee-iscc-spirometry-qa.md
  </files>
  <action>
    For each of the 8 papers in PROJECT.md "Published Work", create a `_publications/YYYY-MM-DD-slug.md` file. Filename dates may be approximate where exact dates are unknown — use month-precision (e.g. `2023-11-01` for KDIR 2023). Filenames listed in `files_modified` are the canonical slugs.

    Front matter schema (use this exact key set so the academicpages publication layout renders cleanly):

    ```yaml
    ---
    title: "<paper title>"
    collection: publications
    permalink: /publication/<slug>
    excerpt: "<one-sentence summary drawn from PROJECT.md context — e.g. 'Open-source Raspberry Pi seeding agri-robot, UK-RAS 2020.'>"
    date: <YYYY-MM-DD>
    venue: "<conference/journal name and year>"
    authors: "<author list with Harry bolded as **Rogers, H.**>"
    paperurl: "<DOI, arXiv, or preprint URL — leave as empty string '' if not known, do NOT fabricate>"
    citations: <integer from PROJECT.md, or 0 if not listed>
    awards: ""    # set to "Best Paper Award" ONLY for the KDIR 2023 entry
    bibtex: |
      @inproceedings{rogers2023kdir,
        author    = {Rogers, Harry and ...},
        title     = {...},
        booktitle = {...},
        year      = {2023}
      }
    ---

    <body>
    ## Abstract
    <2-4 sentence abstract drafted from PROJECT.md context. Mark with a clear comment `<!-- TODO(owner): verify abstract against published version -->` so Harry can revise.>

    {% if page.awards != "" %}<p class="award-badge"><strong>🏆 {{ page.awards }}</strong></p>{% endif %}

    {% include bibtex-block.html %}
    </body>
    ```

    Per-paper specifics (from PROJECT.md):
    1. **2020-04-01-uk-ras-seeding-agri-robot.md** — "An open source seeding agri-robot", UK-RAS 2020, citations 15. Authors: `**Rogers, H.**, et al.`
    2. **2023-04-01-taros-precision-spraying-evaluation.md** — "An automated precision spraying evaluation system", TAROS 2023, citations 4. Best Application *nomination* (note in front matter `awards: "Best Application Nomination"` if PROJECT.md context supports it — per owner profile yes).
    3. **2023-08-01-ieee-case-deposit-identification.md** — "An agricultural precision sprayer deposit identification system", IEEE CASE 2023, citations 3.
    4. **2023-11-01-kdir-interpretable-quantized-cnn.md** — "Evaluating the use of interpretable quantized convolutional neural networks for resource-constrained deployment", KDIR 2023, citations 2. **`awards: "Best Paper Award"` — REQUIRED**.
    5. **2024-02-01-ncaa-domain-specific-augmentations.md** — "Advancing precision agriculture: domain-specific augmentations and robustness testing for convolutional neural networks in precision spraying evaluation", Neural Computing and Applications 2024, citations 9.
    6. **2024-09-01-epia-clock-drawing-test.md** — "Automating the Clock Drawing Test with Deep Learning and Saliency Maps", EPIA 2024. Authors: `Mayne, Sami, la Iglesia, **Rogers, H.**`
    7. **2024-10-01-arxiv-post-spraying-evaluation.md** — "Deep Learning for Precision Agriculture: Post-Spraying Evaluation", arXiv 2024.
    8. **2025-06-08-ieee-iscc-spirometry-qa.md** — "Quality Assurance for Home Spirometry using Machine Learning", IEEE ISCC 2025, citations 1. Authors: `Gardiner, Lines, Wilson, Aung, **Rogers, H.**`

    For unknown DOIs/URLs, set `paperurl: ""` (empty string) and add `<!-- TODO(owner): add DOI/arXiv URL -->` in the body. Do NOT fabricate URLs.

    For each `bibtex:` block, hand-author a minimally valid entry. Use the citation key `rogers<year><venue-slug>` (e.g. `rogers2023kdir`). Author lists in BibTeX use `and`-separated `Last, First` form.
  </action>
  <acceptance_criteria>
    - `ls _publications/*.md | wc -l` equals 8
    - `grep -l "\\*\\*Rogers, H\\.\\*\\*" _publications/*.md | wc -l` equals 8 (Harry bolded in every entry)
    - `grep -l "Best Paper Award" _publications/2023-11-01-kdir-interpretable-quantized-cnn.md | wc -l` equals 1
    - `grep -c "collection: publications" _publications/*.md | awk -F: '{s+=$2} END {print s}'` equals 8
    - `grep -c "bibtex:" _publications/*.md | awk -F: '{s+=$2} END {print s}'` equals 8
    - `grep -c "include bibtex-block.html" _publications/*.md | awk -F: '{s+=$2} END {print s}'` equals 8
    - No fabricated DOIs: for any paper without a known URL, file MUST contain literal `paperurl: ""`
  </acceptance_criteria>
  <verify>
    <automated>bash -c '[ "$(ls _publications/*.md 2>/dev/null | wc -l)" -eq 8 ] && [ "$(grep -l "\\*\\*Rogers, H\\.\\*\\*" _publications/*.md | wc -l)" -eq 8 ] && grep -q "Best Paper Award" _publications/2023-11-01-kdir-interpretable-quantized-cnn.md && [ "$(grep -h "^collection: publications" _publications/*.md | wc -l)" -eq 8 ] && [ "$(grep -h "include bibtex-block.html" _publications/*.md | wc -l)" -eq 8 ]'</automated>
  </verify>
  <done>All 8 publication files exist with required front matter, Harry bolded, KDIR badged.</done>
</task>

<task type="auto">
  <name>Task 2: Create _includes/bibtex-block.html copyable BibTeX include</name>
  <read_first>
    - CLAUDE.md (BibTeX section — render via Liquid include from front matter)
    - any existing _includes/*.html for style reference
  </read_first>
  <files>_includes/bibtex-block.html</files>
  <action>
    Create a small Liquid include that renders `page.bibtex` inside a `<pre><code>` block with a heading and a hint for the reader. No JavaScript required — browsers handle text selection natively.

    ```liquid
    {% if page.bibtex %}
    <details class="bibtex" markdown="0">
      <summary>BibTeX</summary>
      <pre><code class="language-bibtex">{{ page.bibtex }}</code></pre>
    </details>
    {% endif %}
    ```

    Use `<details>`/`<summary>` so the block is collapsible — keeps publication pages compact while remaining copyable on expand.
  </action>
  <acceptance_criteria>
    - `test -f _includes/bibtex-block.html`
    - `grep -c "page.bibtex" _includes/bibtex-block.html` is >= 1
    - `grep -c "<pre>" _includes/bibtex-block.html` is >= 1
    - `grep -c "<summary>BibTeX</summary>" _includes/bibtex-block.html` equals 1
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test -f _includes/bibtex-block.html && grep -q "page.bibtex" _includes/bibtex-block.html && grep -q "<pre>" _includes/bibtex-block.html'</automated>
  </verify>
  <done>BibTeX include exists and is referenced from each publication file (Task 1 acceptance enforces this).</done>
</task>

<task type="auto">
  <name>Task 3: Verify /publications/ listing page renders entries reverse-chronologically</name>
  <read_first>
    - _pages/publications.html (existing academicpages listing layout)
  </read_first>
  <files>_pages/publications.html</files>
  <action>
    Inspect `_pages/publications.html`. The academicpages default iterates `site.publications` and groups by year. Verify it does this; if not, edit so it does. Required behaviour:
    - Iterates `site.publications` sorted by `date` descending (reverse-chronological — PUBS-01)
    - Groups by year (typical academicpages pattern: `{% for post in site.publications reversed %}` or `{% assign sorted = site.publications | sort: 'date' | reverse %}`)
    - Renders each entry's title, authors, venue, year, link, and a per-entry permalink to the detail page

    If the academicpages default already satisfies this, only add a comment at the top: `<!-- Verified reverse-chronological listing per PUBS-01. -->` and do not otherwise modify. Do not change layouts under `_layouts/` — that may regress academicpages internals.
  </action>
  <acceptance_criteria>
    - `test -f _pages/publications.html`
    - `grep -cE "site\\.publications|publications.*sort|publications.*reverse" _pages/publications.html` is >= 1
    - Layout still has `permalink: /publications/` (or similar) front matter intact
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test -f _pages/publications.html && grep -qE "site\\.publications" _pages/publications.html'</automated>
  </verify>
  <done>Publications listing iterates site.publications in reverse-chronological order.</done>
</task>

</tasks>

<verification>
After this plan, ROADMAP success criterion 3 (8 papers, reverse-chrono, Harry bolded, BibTeX, KDIR Best Paper badge) is satisfied.
</verification>

<success_criteria>
- 8 publication Markdown files exist with required front matter
- BibTeX include exists and is referenced by every publication
- KDIR 2023 carries Best Paper Award badge
- /publications/ listing iterates in reverse-chronological order
</success_criteria>

<output>
Create `.planning/phases/02-content-population/02-04-SUMMARY.md` when done. List any TODO(owner) markers added (abstracts, DOIs) so Harry can sweep them in one pass.
</output>
