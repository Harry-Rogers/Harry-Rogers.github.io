---
phase: 02-content-population
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - _posts/
  - _drafts/
  - _publications/
  - _talks/
  - _teaching/
  - _portfolio/
  - _pages/cv.md
  - _pages/cv-json.md
  - _pages/markdown.md
  - _pages/non-menu-page.md
  - _pages/terms.md
  - _pages/archive-layout-with-content.md
  - _data/cv.json
autonomous: true
requirements:
  - POLISH-06
must_haves:
  truths:
    - "No demo/lorem/placeholder text remains anywhere in the working tree"
    - "No _posts/ or _drafts/ directories remain (structural enforcement of no-blog)"
    - "Content collection directories exist but contain no demo entries"
  artifacts:
    - path: "_publications/"
      provides: "Empty publications collection ready for real entries"
    - path: "_talks/"
      provides: "Empty talks collection ready for real entries"
    - path: "_teaching/"
      provides: "Empty teaching collection ready for real entries"
    - path: "_portfolio/"
      provides: "Empty portfolio collection ready for real entries"
  key_links:
    - from: "repo root"
      to: "grep gate"
      via: "purge verification"
      pattern: "lorem|sample text|cras mattis|paper title number|john doe"
---

<objective>
Purge ALL academicpages demo content from the working tree. This is the FIRST plan in Phase 2 (per ROADMAP success criterion 1 — purge BEFORE authoring real content to prevent contamination). After this plan completes, a grep for placeholder strings returns zero matches.

Purpose: Clean slate so subsequent plans can author real content without accidentally inheriting/mixing demo strings.
Output: Empty collection directories; no _posts/, no _drafts/; demo _pages removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete _posts/, _drafts/, demo collection entries, demo pages, demo cv data</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 2 success criterion 1)
    - CLAUDE.md (Locked decisions: no _posts/, no _news/, no jekyll-scholar)
  </read_first>
  <files>
    _posts/, _drafts/, _publications/*.md, _talks/*.md, _teaching/*.md, _portfolio/*, _pages/cv.md, _pages/cv-json.md, _pages/markdown.md, _pages/non-menu-page.md, _pages/terms.md, _pages/archive-layout-with-content.md, _data/cv.json
  </files>
  <action>
    Run, from repo root:
    - `rm -rf _posts _drafts`
    - `rm -f _publications/2009-10-01-paper-title-number-1.md _publications/2010-10-01-paper-title-number-2.md _publications/2015-10-01-paper-title-number-3.md _publications/2024-02-17-paper-title-number-4.md _publications/2025-06-08-paper-title-number-5.md`
    - `rm -f _talks/2012-03-01-talk-1.md _talks/2013-03-01-tutorial-1.md _talks/2014-02-01-talk-2.md _talks/2014-03-01-talk-3.md`
    - `rm -f _teaching/2014-spring-teaching-1.md _teaching/2015-spring-teaching-2.md`
    - `rm -f _portfolio/portfolio-1.md _portfolio/portfolio-2.html`
    - `rm -f _pages/cv.md _pages/cv-json.md _pages/markdown.md _pages/non-menu-page.md _pages/terms.md _pages/archive-layout-with-content.md`
    - `rm -f _data/cv.json`
    Do NOT delete: _pages/about.md, _pages/publications.html, _pages/talks.html, _pages/teaching.html, _pages/portfolio.html, _pages/404.md, _pages/sitemap.md, archive layout helpers (year-archive.html, tag-archive.html, category-archive.html, page-archive.html, collection-archive.html, talkmap.html). These are listing/layout pages with no lorem content — Plan 02-02 will repurpose them. Keep all _data/authors.yml, _data/navigation.yml, _data/ui-text.yml (navigation cleanup happens in Plan 02-09).
    Also stage `_teaching/profile-original.jpg` — it should be at `assets/images/profile-original.jpg`, not in `_teaching/`. Move it: `mv _teaching/profile-original.jpg assets/images/profile-original.jpg` (if assets/images/profile-original.jpg already exists, prefer the existing one and `rm _teaching/profile-original.jpg`).
  </action>
  <acceptance_criteria>
    - `! test -d _posts` succeeds (directory removed)
    - `! test -d _drafts` succeeds (directory removed)
    - `ls _publications/ 2>/dev/null | wc -l` equals 0
    - `ls _talks/ 2>/dev/null | wc -l` equals 0
    - `ls _teaching/ 2>/dev/null | wc -l` equals 0
    - `ls _portfolio/ 2>/dev/null | wc -l` equals 0
    - `! test -f _pages/cv.md` and `! test -f _pages/cv-json.md` and `! test -f _pages/markdown.md`
    - `test -f assets/images/profile-original.jpg`
    - `! test -f _teaching/profile-original.jpg`
  </acceptance_criteria>
  <verify>
    <automated>bash -c '! test -d _posts && ! test -d _drafts && [ "$(ls _publications/ 2>/dev/null | wc -l)" -eq 0 ] && [ "$(ls _talks/ 2>/dev/null | wc -l)" -eq 0 ] && [ "$(ls _teaching/ 2>/dev/null | wc -l)" -eq 0 ] && [ "$(ls _portfolio/ 2>/dev/null | wc -l)" -eq 0 ] && ! test -f _pages/cv.md && ! test -f _pages/markdown.md && test -f assets/images/profile-original.jpg'</automated>
  </verify>
  <done>All demo collection files, demo pages, _posts/, _drafts/, and _data/cv.json removed; collection directories empty; headshot relocated.</done>
</task>

<task type="auto">
  <name>Task 2: Run grep purge gate to assert zero placeholder strings remain</name>
  <read_first>
    - .planning/ROADMAP.md (Phase 2 success criterion 1 — exact grep pattern)
  </read_first>
  <files>
    (no file modifications — verification only)
  </files>
  <action>
    Run the canonical purge gate from repo root, excluding `.git/`, `.planning/`, `_site/`, `README.md`, and `CLAUDE.md` (these are non-build artefacts or planning documents):

    `grep -riE "lorem|sample text|cras mattis|paper title number|john doe|teaching experience [0-9]" . --exclude-dir=.git --exclude-dir=.planning --exclude-dir=_site --exclude-dir=node_modules --exclude=README.md --exclude=CLAUDE.md`

    Result MUST be empty (exit code 1 from grep = no matches found). If matches surface in any other directory (e.g. `_includes/`, `_layouts/`, `_sass/`, `assets/`), inspect each match:
    - If it is genuine template scaffolding (e.g. variable names, CSS class names), leave it.
    - If it is human-readable demo prose, delete or rewrite to remove the placeholder string.

    Document any false-positives (template variable names containing "sample") in the plan SUMMARY.
  </action>
  <acceptance_criteria>
    - `grep -riE "lorem|sample text|cras mattis|paper title number|john doe|teaching experience [0-9]" . --exclude-dir=.git --exclude-dir=.planning --exclude-dir=_site --exclude-dir=node_modules --exclude=README.md --exclude=CLAUDE.md` exits with code 1 (no matches)
  </acceptance_criteria>
  <verify>
    <automated>bash -c '! grep -riE "lorem|sample text|cras mattis|paper title number|john doe|teaching experience [0-9]" . --exclude-dir=.git --exclude-dir=.planning --exclude-dir=_site --exclude-dir=node_modules --exclude=README.md --exclude=CLAUDE.md'</automated>
  </verify>
  <done>Grep purge gate returns zero matches; phase can proceed to content authoring.</done>
</task>

</tasks>

<verification>
After this plan, all demo content is gone and the grep gate is satisfied. No real content has been authored yet — that is Plans 02-02 through 02-10.
</verification>

<success_criteria>
- Demo content directories empty or removed
- Grep purge gate (ROADMAP success criterion 1) returns zero matches
- Headshot located at assets/images/profile-original.jpg
</success_criteria>

<output>
Create `.planning/phases/02-content-population/02-01-SUMMARY.md` when done, noting any false-positive grep matches that needed handling.
</output>
