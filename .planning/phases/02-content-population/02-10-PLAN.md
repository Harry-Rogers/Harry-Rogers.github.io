---
phase: 02-content-population
plan: 10
type: execute
wave: 3
depends_on:
  - 02-03
  - 02-09
files_modified:
  - _config.yml
autonomous: true
requirements:
  - CONTACT-02
  - CONTACT-03
must_haves:
  truths:
    - "Author sidebar has FontAwesome icon links for ORCID, Google Scholar, GitHub, LinkedIn, Oxford department page"
    - "No iframes anywhere"
    - "No embedded citation widgets (Scholar/ORCID iframes are blocked or break mobile per CLAUDE.md decision)"
  artifacts:
    - path: "_config.yml"
      provides: "author.orcid, author.googlescholar, author.github, author.linkedin, author.uri (Oxford), author.email"
      contains: "0000-0003-3227-5677"
  key_links:
    - from: "_config.yml"
      to: "minimal-mistakes _includes/author-profile-custom-links.html (academicpages sidebar renderer)"
      via: "FontAwesome icon map"
      pattern: "fa-orcid|fa-google-scholar|fa-github|fa-linkedin"
---

<objective>
Verify and (if needed) populate the author block in `_config.yml` with all CONTACT-02 links — ORCID, Google Scholar, GitHub, LinkedIn, Oxford department — so the academicpages author-profile sidebar renders them as FontAwesome icon links. Confirm CONTACT-03 by greppling for forbidden iframe markers.

Purpose: CONTACT-02 (sidebar links present), CONTACT-03 (icon-link strategy, no widgets).
Output: Updated _config.yml; grep audit that no iframes exist.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
@_config.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate author contact links in _config.yml</name>
  <read_first>
    - _config.yml (existing author block; planning_context says url/repo/title/author already partially populated)
    - .planning/PROJECT.md (Owner Profile table — exact ORCID, Scholar, GitHub, LinkedIn, Oxford page values)
    - .planning/REQUIREMENTS.md (CONTACT-02 exact link list)
  </read_first>
  <files>_config.yml</files>
  <action>
    In `_config.yml`, locate the `author:` mapping. Ensure these keys are set to these exact values (using academicpages' conventional key names — verify by reading the existing file for the right key names; academicpages typically uses `orcid`, `googlescholar`, `github`, `linkedin`, `uri`, `email`):

    - `author.email: "Harry.Rogers@eng.ox.ac.uk"`
    - `author.orcid: "https://orcid.org/0000-0003-3227-5677"`
    - `author.googlescholar: "https://scholar.google.com/citations?user=sPwcwvMAAAAJ"`
    - `author.github: "Harry-Rogers"`
    - `author.linkedin: "harry-rogers-ox"` (academicpages strips the `in/` and the bare username; verify against existing demo author block format)
    - `author.uri: "https://eng.ox.ac.uk/people/harry-rogers"` (Oxford department page — academicpages renders `uri` as a "Website" icon link in the sidebar)

    Do NOT add `twitter` (PROJECT.md: no Twitter). Remove any demo values still present (e.g. `johndoe`, `john-doe`).

    Do not add HTML email obfuscation here — that is CONTACT-01 in Phase 4. The `author.email` key in academicpages renders as a `mailto:` icon in the sidebar; that's expected for the sidebar but the landing page body must NOT carry a raw mailto until Phase 4 obfuscates it.
  </action>
  <acceptance_criteria>
    - `grep -c "0000-0003-3227-5677" _config.yml` is >= 1
    - `grep -c "sPwcwvMAAAAJ" _config.yml` is >= 1
    - `grep -c "Harry-Rogers" _config.yml` is >= 1
    - `grep -c "harry-rogers-ox" _config.yml` is >= 1
    - `grep -c "eng.ox.ac.uk/people/harry-rogers" _config.yml` is >= 1
    - `grep -c "Harry.Rogers@eng.ox.ac.uk" _config.yml` is >= 1
    - No demo author values: `! grep -iE "john doe|johndoe|john-doe" _config.yml`
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -q "0000-0003-3227-5677" _config.yml && grep -q "sPwcwvMAAAAJ" _config.yml && grep -q "Harry-Rogers" _config.yml && grep -q "harry-rogers-ox" _config.yml && grep -q "eng.ox.ac.uk/people/harry-rogers" _config.yml && grep -q "Harry.Rogers@eng.ox.ac.uk" _config.yml && ! grep -iqE "john doe|johndoe|john-doe" _config.yml'</automated>
  </verify>
  <done>All CONTACT-02 author links populated in _config.yml with real values.</done>
</task>

<task type="auto">
  <name>Task 2: CONTACT-03 audit — assert no iframes or embedded widgets</name>
  <read_first>
    - CLAUDE.md (CONTACT-03 rationale — Scholar blocks iframes; ORCID widget breaks mobile)
  </read_first>
  <files>(no modifications — verification only)</files>
  <action>
    Grep the working tree for forbidden iframe / widget markers. Exclude `.git/`, `.planning/`, `_site/`, `node_modules/`, `_sass/minimal-mistakes/` (vendored theme), README.md, CLAUDE.md (these are non-build or planning docs):

    `grep -riE "<iframe|orcid.org/.*widget|scholar.*badge|impactstory" . --exclude-dir=.git --exclude-dir=.planning --exclude-dir=_site --exclude-dir=node_modules --exclude-dir=_sass --exclude=README.md --exclude=CLAUDE.md`

    Expected: zero matches. If any surface in `_includes/` or `_layouts/` originate from the vendored academicpages template (not our Phase 2 work), document them in 02-10-SUMMARY.md but do NOT delete (touching the theme is out of Phase 2 scope). If any are in files Phase 2 wrote, delete them.
  </action>
  <acceptance_criteria>
    - `grep -riE "<iframe|orcid.org/.*widget|scholar.*badge|impactstory" . --exclude-dir=.git --exclude-dir=.planning --exclude-dir=_site --exclude-dir=node_modules --exclude-dir=_sass --exclude=README.md --exclude=CLAUDE.md` exits with code 1 (no matches), OR all matches are in vendored theme files outside Phase 2 scope and have been documented in the SUMMARY
  </acceptance_criteria>
  <verify>
    <automated>bash -c '! grep -riE "<iframe|orcid.org/.*widget|scholar.*badge|impactstory" . --exclude-dir=.git --exclude-dir=.planning --exclude-dir=_site --exclude-dir=node_modules --exclude-dir=_sass --exclude=README.md --exclude=CLAUDE.md'</automated>
  </verify>
  <done>No iframes/widgets in non-vendored files; any vendored matches documented.</done>
</task>

</tasks>

<verification>
CONTACT-02 and CONTACT-03 reachable; ROADMAP criterion 4's "author profile sidebar shows ORCID/Scholar/GitHub/LinkedIn/Oxford as FontAwesome icon links, no iframes" requirement satisfied.
</verification>

<success_criteria>
- _config.yml author block has all 5 contact links + email
- No iframes or embedded widgets in Phase-2-authored files
</success_criteria>

<output>
Create `.planning/phases/02-content-population/02-10-SUMMARY.md` when done. Document any vendored-theme iframe matches if found.
</output>
