---
phase: 02-content-population
plan: 05
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - _talks/2023-04-01-taros-precision-spraying.md
  - _talks/2023-11-01-kdir-interpretable-quantized.md
  - _talks/2024-09-01-epia-clock-drawing.md
  - _talks/2025-06-08-ieee-iscc-spirometry.md
  - _talks/2024-XX-agriforwards-chair.md
  - _talks/2024-XX-ormiston-victory-careers.md
  - _pages/talks.html
autonomous: true
requirements:
  - TALKS-01
  - TALKS-02
  - TALKS-03
must_haves:
  truths:
    - "Each known conference talk has a corresponding _talks/ entry"
    - "Each entry has title, venue, date, location"
    - "Chaired sessions (AgriFoRwArdS CDT conferences) appear"
    - "Career talks (Ormiston Victory Academy) appear"
    - "/talks/ page lists entries in reverse-chronological order"
  artifacts:
    - path: "_talks/"
      provides: "Real talk entries replacing demo content"
  key_links:
    - from: "_talks/*.md"
      to: "_pages/talks.html"
      via: "site.talks iteration"
      pattern: "site\\.talks"
---

<objective>
Author talk entries in `_talks/` corresponding to (a) conference paper presentations matched to publications and (b) chaired sessions and career talks. Use the academicpages talk front matter schema.

Purpose: TALKS-01..03 — talks page is a credibility surface and shows research dissemination.
Output: 4-6 talk markdown files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author conference talk entries for paper-linked presentations</name>
  <read_first>
    - .planning/PROJECT.md (Published Work — papers presumed presented at their venue; Talks list in planning_context: TAROS 2023, KDIR 2023, IEEE ISCC 2025, EPIA 2024)
    - .planning/REQUIREMENTS.md (TALKS-01, TALKS-02 — required fields)
  </read_first>
  <files>
    _talks/2023-04-01-taros-precision-spraying.md,
    _talks/2023-11-01-kdir-interpretable-quantized.md,
    _talks/2024-09-01-epia-clock-drawing.md,
    _talks/2025-06-08-ieee-iscc-spirometry.md
  </files>
  <action>
    For each paper Harry first-authored that he presented (assume he presented his own first-author papers), create a `_talks/YYYY-MM-DD-slug.md` with academicpages talk front matter:

    ```yaml
    ---
    title: "<talk title — usually paper title>"
    collection: talks
    type: "Conference proceedings talk"
    permalink: /talks/<slug>
    venue: "<conference full name>"
    date: <YYYY-MM-DD>
    location: "<city, country — leave blank with TODO(owner) if unknown>"
    paper: "/publication/<linked-publication-slug>"
    ---

    <body>
    Conference presentation of the paper: [<paper title>](/publication/<slug>).

    <!-- TODO(owner): add slides/poster link if available, plus location -->
    </body>
    ```

    Map each file to its paper slug from Plan 02-04. For EPIA 2024 (Clock Drawing Test) Harry is co-author, not first author — note in body "Co-authored presentation" but still include since per PROJECT.md it's a relevant talk venue. If unsure whether Harry actually presented, add `<!-- TODO(owner): confirm presenter -->`.

    Do NOT fabricate locations — use `location: ""` with a TODO marker.
  </action>
  <acceptance_criteria>
    - `ls _talks/2023-*.md 2>/dev/null | wc -l` is >= 2 (TAROS, KDIR)
    - `ls _talks/2024-*.md 2>/dev/null | wc -l` is >= 1 (EPIA)
    - `ls _talks/2025-*.md 2>/dev/null | wc -l` is >= 1 (ISCC)
    - Every file in _talks/ matching `*.md` contains `collection: talks` and `type:` keys
  </acceptance_criteria>
  <verify>
    <automated>bash -c '[ "$(ls _talks/2023-*.md 2>/dev/null | wc -l)" -ge 2 ] && [ "$(ls _talks/2024-*.md 2>/dev/null | wc -l)" -ge 1 ] && [ "$(ls _talks/2025-*.md 2>/dev/null | wc -l)" -ge 1 ] && [ "$(grep -l "collection: talks" _talks/*.md | wc -l)" = "$(ls _talks/*.md | wc -l)" ]'</automated>
  </verify>
  <done>Conference talk entries for TAROS 2023, KDIR 2023, EPIA 2024, IEEE ISCC 2025 exist.</done>
</task>

<task type="auto">
  <name>Task 2: Author chaired sessions and career talk entries</name>
  <read_first>
    - .planning/PROJECT.md (planning_context: chaired sessions at AgriFoRwArdS CDT conferences, Ormiston Victory career talk)
    - .planning/REQUIREMENTS.md (TALKS-03 — chaired sessions; TALKS-01 — career talks)
  </read_first>
  <files>
    _talks/2024-XX-agriforwards-chair.md,
    _talks/2024-XX-ormiston-victory-careers.md
  </files>
  <action>
    Create two additional entries. Use approximate dates with month precision (e.g. 2024-06-01) and add TODO markers for exact dates.

    File `_talks/2024-06-01-agriforwards-chair.md` (rename if exact date known):
    ```yaml
    ---
    title: "Session Chair — AgriFoRwArdS CDT Annual Conference"
    collection: talks
    type: "Session chair"
    permalink: /talks/agriforwards-chair-2024
    venue: "AgriFoRwArdS CDT Annual Conference"
    date: 2024-06-01
    location: "Lincoln, UK"
    ---

    Chaired presentation sessions at the AgriFoRwArdS CDT Annual Conference. Also served on the Programme Committee for the 2024 Annual Conference and Summer School.

    <!-- TODO(owner): confirm exact date and add additional chaired sessions across years -->
    ```

    File `_talks/2024-03-01-ormiston-victory-careers.md` (placeholder date — confirm):
    ```yaml
    ---
    title: "Careers in Engineering Research"
    collection: talks
    type: "Outreach talk"
    permalink: /talks/ormiston-victory-careers
    venue: "Ormiston Victory Academy"
    date: 2024-03-01
    location: "Norfolk, UK"
    ---

    Career talk to secondary-school students on pathways into engineering research and AI.

    <!-- TODO(owner): confirm date -->
    ```

    Rename filenames to match real dates when known.
  </action>
  <acceptance_criteria>
    - At least one file in _talks/ contains `type: "Session chair"` or `Session chair` literal
    - At least one file in _talks/ contains `Ormiston Victory` literal
    - `grep -l "AgriFoRwArdS" _talks/*.md | wc -l` is >= 1
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -lq "Session chair" _talks/*.md && grep -lq "Ormiston Victory" _talks/*.md && grep -lq "AgriFoRwArdS" _talks/*.md'</automated>
  </verify>
  <done>Chaired sessions and Ormiston Victory career talk entries exist.</done>
</task>

<task type="auto">
  <name>Task 3: Verify /talks/ listing renders reverse-chronologically</name>
  <read_first>
    - _pages/talks.html (existing academicpages listing layout)
  </read_first>
  <files>_pages/talks.html</files>
  <action>
    Inspect `_pages/talks.html`. Verify it iterates `site.talks` in reverse-chronological order (academicpages default). If it does, add a one-line comment `<!-- Verified reverse-chronological per TALKS-01. -->` and stop. Do not modify the layout otherwise.
  </action>
  <acceptance_criteria>
    - `test -f _pages/talks.html`
    - `grep -q "site.talks" _pages/talks.html`
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test -f _pages/talks.html && grep -q "site.talks" _pages/talks.html'</automated>
  </verify>
  <done>Talks listing page intact and verified.</done>
</task>

</tasks>

<verification>
TALKS-01 (listing), TALKS-02 (per-entry fields), TALKS-03 (chaired sessions) reachable; ROADMAP criterion 4 partially satisfied.
</verification>

<success_criteria>
- 4-6 talk entries in _talks/
- Each entry has title, venue, date, location (or TODO marker for location)
- Chaired sessions and career talks distinguished by `type:` field
</success_criteria>

<output>
Create `.planning/phases/02-content-population/02-05-SUMMARY.md` when done. List TODOs for owner sweep.
</output>
