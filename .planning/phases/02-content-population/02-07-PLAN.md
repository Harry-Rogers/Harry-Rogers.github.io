---
phase: 02-content-population
plan: 07
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - _portfolio/precision-spraying-closing-the-loop.md
  - _portfolio/clock-drawing-test.md
  - _portfolio/home-spirometry-qa.md
  - _portfolio/picar.md
  - _pages/portfolio.html
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
must_haves:
  truths:
    - "/portfolio/ page lists 4 real projects"
    - "Precision Spraying (Closing The Loop), Clock Drawing Test, Home Spirometry QA, PiCar all represented"
    - "Each project links to relevant publications or code"
  artifacts:
    - path: "_portfolio/"
      provides: "4 real project entries"
  key_links:
    - from: "_portfolio/*.md"
      to: "_publications/*"
      via: "permalinks in body"
      pattern: "/publication/"
---

<objective>
Author 4 project entries in `_portfolio/` covering the projects named in PROJ-02.

Purpose: PROJ-01, PROJ-02 — portfolio is the "show, don't just list" surface for research projects.
Output: 4 portfolio markdown files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author 4 portfolio project entries</name>
  <read_first>
    - .planning/REQUIREMENTS.md (PROJ-02 — exact 4 projects)
    - .planning/PROJECT.md (Owner Profile — PhD thesis title "Closing The Loop on Precision Spraying"; Published Work for paper links)
  </read_first>
  <files>
    _portfolio/precision-spraying-closing-the-loop.md,
    _portfolio/clock-drawing-test.md,
    _portfolio/home-spirometry-qa.md,
    _portfolio/picar.md
  </files>
  <action>
    Academicpages portfolio front matter (layout: single, collection: portfolio):

    ```yaml
    ---
    title: "<project title>"
    excerpt: "<one-sentence>"
    collection: portfolio
    permalink: /portfolio/<slug>
    ---

    <body — 2-4 paragraphs covering: what the project was, why it mattered, what was contributed, links to papers/code>
    ```

    Per-entry:

    1. `_portfolio/precision-spraying-closing-the-loop.md` — "Closing The Loop on Precision Spraying". DPhil thesis topic, AgriFoRwArdS CDT × Syngenta. Cover automated sprayer evaluation, deposit identification, interpretable quantized CNNs. Link to publications: TAROS 2023, IEEE CASE 2023, KDIR 2023, NCAA 2024, arXiv 2024 (use `/publication/<slug>` paths matching Plan 02-04 filenames).

    2. `_portfolio/clock-drawing-test.md` — "Automating the Clock Drawing Test with Deep Learning and Saliency Maps". Medical-AI imaging work — collaboration on EPIA 2024 paper. Link to `/publication/2024-09-01-epia-clock-drawing`.

    3. `_portfolio/home-spirometry-qa.md` — "Quality Assurance for Home Spirometry". Machine learning for at-home spirometry signal quality — IEEE ISCC 2025. Link to `/publication/2025-06-08-ieee-iscc-spirometry-qa`.

    4. `_portfolio/picar.md` — "PiCar — open-source seeding agri-robot". Raspberry-Pi-based agricultural robot from MSc/early PhD era. Link to UK-RAS 2020 paper `/publication/2020-04-01-uk-ras-seeding-agri-robot` and to GitHub repo if known (use `<!-- TODO(owner): add repo URL -->` if not).

    For each entry, draft 2-4 paragraphs from PROJECT.md context. Mark factual claims that cannot be verified from PROJECT.md with `<!-- TODO(owner): verify -->`.
  </action>
  <acceptance_criteria>
    - `ls _portfolio/*.md | wc -l` equals 4
    - `grep -l "Closing The Loop" _portfolio/*.md | wc -l` is >= 1
    - `grep -l "Clock Drawing" _portfolio/*.md | wc -l` is >= 1
    - `grep -l "Spirometry" _portfolio/*.md | wc -l` is >= 1
    - `grep -l "PiCar" _portfolio/*.md | wc -l` is >= 1
    - Every file has `collection: portfolio`: `grep -L "collection: portfolio" _portfolio/*.md` is empty
    - At least 5 cross-links to `/publication/`: `grep -h "/publication/" _portfolio/*.md | wc -l` is >= 5
  </acceptance_criteria>
  <verify>
    <automated>bash -c '[ "$(ls _portfolio/*.md 2>/dev/null | wc -l)" -eq 4 ] && grep -lq "Closing The Loop" _portfolio/*.md && grep -lq "Clock Drawing" _portfolio/*.md && grep -lq "Spirometry" _portfolio/*.md && grep -lq "PiCar" _portfolio/*.md && [ -z "$(grep -L "collection: portfolio" _portfolio/*.md)" ] && [ "$(grep -h "/publication/" _portfolio/*.md | wc -l)" -ge 5 ]'</automated>
  </verify>
  <done>4 portfolio entries exist with required topics and cross-links to publications.</done>
</task>

<task type="auto">
  <name>Task 2: Verify /portfolio/ listing layout intact</name>
  <read_first>
    - _pages/portfolio.html
  </read_first>
  <files>_pages/portfolio.html</files>
  <action>
    Inspect `_pages/portfolio.html`. Confirm it iterates `site.portfolio` (academicpages default). Add `<!-- Verified per PROJ-01. -->` comment. Do not otherwise modify.
  </action>
  <acceptance_criteria>
    - `test -f _pages/portfolio.html`
    - `grep -q "site.portfolio" _pages/portfolio.html`
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test -f _pages/portfolio.html && grep -q "site.portfolio" _pages/portfolio.html'</automated>
  </verify>
  <done>Portfolio listing intact.</done>
</task>

</tasks>

<verification>
PROJ-01, PROJ-02 reachable; ROADMAP criterion 4 satisfied for /portfolio/.
</verification>

<success_criteria>
- 4 portfolio entries for the four required projects
- Cross-links to publications present
</success_criteria>

<output>
Create `.planning/phases/02-content-population/02-07-SUMMARY.md` when done.
</output>
