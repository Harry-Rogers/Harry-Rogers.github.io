---
phase: 02-content-population
plan: 02
type: execute
wave: 3
depends_on:
  - 02-01
  - 02-03
  - 02-08
files_modified:
  - _pages/about.md
  - _includes/news-widget.html
  - _includes/now-recently-previously.html
autonomous: true
requirements:
  - BIO-01
  - BIO-02
  - BIO-03
must_haves:
  truths:
    - "Landing page shows Harry's name, role (Postdoctoral Research Associate, IBME, Oxford), and supervisor/group (Prof. Alison Noble, Biomedical Image Analysis)"
    - "Landing page contains a 2-4 paragraph bio framed as 'human-AI collaboration in medical AI broadly, currently medical education'"
    - "Landing page renders a Now/Recently/Previously three-block widget (medical education → medical imaging → precision agriculture)"
    - "Landing page renders the latest 3-5 news entries from _data/news.yml"
  artifacts:
    - path: "_pages/about.md"
      provides: "Landing page front matter + bio + widget includes"
      contains: "human-AI collaboration in medical AI"
    - path: "_includes/now-recently-previously.html"
      provides: "Three-block research-framing widget"
    - path: "_includes/news-widget.html"
      provides: "Liquid block that iterates site.data.news"
  key_links:
    - from: "_pages/about.md"
      to: "_includes/now-recently-previously.html"
      via: "{% include now-recently-previously.html %}"
      pattern: "include now-recently-previously"
    - from: "_pages/about.md"
      to: "_data/news.yml"
      via: "{% include news-widget.html %}"
      pattern: "include news-widget"
---

<objective>
Author the landing page (`_pages/about.md`) with Harry's real bio, a Now/Recently/Previously research-framing widget, and a News widget that reads `_data/news.yml`. This is the single most important content surface — it carries ROADMAP success criterion 2.

Purpose: Within 10 seconds a visitor must learn who Harry is, what he works on, and how to reach him.
Output: Updated about.md + two reusable includes for the widgets.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@CLAUDE.md
@_pages/about.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Author _pages/about.md bio (2-4 paragraphs) and identity</name>
  <read_first>
    - _pages/about.md (existing front matter — preserve permalink, layout)
    - .planning/PROJECT.md (Owner Profile table, Core Value, Research focus)
    - .planning/REQUIREMENTS.md (BIO-01, BIO-02 exact wording)
  </read_first>
  <files>_pages/about.md</files>
  <action>
    Rewrite `_pages/about.md` body content. Preserve front matter keys: `permalink: /`, `title: "About"` (or omit title to use site title), `layout: single`, `author_profile: true`. Replace demo body with:

    Paragraph 1 — Identity opener: "I'm Harry Rogers, a Postdoctoral Research Associate at the Institute of Biomedical Engineering (IBME), University of Oxford, working in Prof. Alison Noble's Biomedical Image Analysis group."

    Paragraph 2 — Current research framing (MUST contain literal string `human-AI collaboration in medical AI`): explain that Harry's broad research interest is human-AI collaboration in medical AI, currently focused on AI in medical education. Mention this reconciles the imaging-vs-education tension by framing both as instances of the same broader question.

    Paragraph 3 — Prior medical-AI work: Clock Drawing Test automation (EPIA 2024) and Home Spirometry QA (IEEE ISCC 2025) as prior medical-imaging applications.

    Paragraph 4 — Background: DPhil at the AgriFoRwArdS CDT (UEA, supervised by Prof. Beatriz De La Iglesia, in collaboration with Syngenta), thesis "Closing The Loop on Precision Spraying"; before that MSc Robotics & Autonomous Systems (UEA) and BSc Computer Science (Lincoln).

    Below the bio, insert these two Liquid include lines (separated by `## Now / Recently / Previously` and `## News` H2 headings respectively):

    ```
    ## Now / Recently / Previously
    {% include now-recently-previously.html %}

    ## News
    {% include news-widget.html %}
    ```

    Do NOT add a raw `mailto:` — email obfuscation is Phase 4 (CONTACT-01).
  </action>
  <acceptance_criteria>
    - `grep -c "human-AI collaboration in medical AI" _pages/about.md` is >= 1
    - `grep -c "Postdoctoral Research Associate" _pages/about.md` is >= 1
    - `grep -c "Alison Noble" _pages/about.md` is >= 1
    - `grep -c "AgriFoRwArdS" _pages/about.md` is >= 1
    - `grep -c "include now-recently-previously.html" _pages/about.md` equals 1
    - `grep -c "include news-widget.html" _pages/about.md` equals 1
    - `grep -c "permalink: /" _pages/about.md` is >= 1
    - No `mailto:` literal: `! grep -q "mailto:" _pages/about.md`
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'grep -q "human-AI collaboration in medical AI" _pages/about.md && grep -q "Postdoctoral Research Associate" _pages/about.md && grep -q "Alison Noble" _pages/about.md && grep -q "AgriFoRwArdS" _pages/about.md && grep -q "include now-recently-previously.html" _pages/about.md && grep -q "include news-widget.html" _pages/about.md && ! grep -q "mailto:" _pages/about.md'</automated>
  </verify>
  <done>Landing page bio reflects Harry's real identity and research framing; widget includes wired.</done>
</task>

<task type="auto">
  <name>Task 2: Create _includes/now-recently-previously.html three-block widget</name>
  <read_first>
    - .planning/PROJECT.md (Owner Profile — Research focus, Prior interests)
    - .planning/REQUIREMENTS.md (BIO-03 — three-block widget linking to publications)
  </read_first>
  <files>_includes/now-recently-previously.html</files>
  <action>
    Create a new include with three labelled blocks, plain HTML + minimal inline classes (theme styling is Phase 3 — do not add custom Sass). Structure:

    ```html
    <div class="now-recently-previously" markdown="0">
      <div class="block">
        <h3>Now</h3>
        <p><strong>Medical education AI.</strong> Postdoctoral work in the Noble group on human-AI collaboration in medical training and assessment contexts.</p>
      </div>
      <div class="block">
        <h3>Recently</h3>
        <p><strong>Medical imaging AI.</strong> Clock Drawing Test automation with saliency maps (EPIA 2024) and quality assurance for home spirometry (IEEE ISCC 2025). <a href="/publications/">See publications →</a></p>
      </div>
      <div class="block">
        <h3>Previously</h3>
        <p><strong>Precision agriculture.</strong> DPhil thesis "Closing The Loop on Precision Spraying" (AgriFoRwArdS CDT, UEA + Syngenta) — sprayer evaluation, deposit identification, interpretable quantized CNNs. <a href="/publications/">See publications →</a></p>
      </div>
    </div>
    ```

    Each "Recently" and "Previously" block MUST contain at least one link to `/publications/` (BIO-03 requires blocks to link to relevant publications).
  </action>
  <acceptance_criteria>
    - `test -f _includes/now-recently-previously.html`
    - `grep -c "<h3>Now</h3>" _includes/now-recently-previously.html` equals 1
    - `grep -c "<h3>Recently</h3>" _includes/now-recently-previously.html` equals 1
    - `grep -c "<h3>Previously</h3>" _includes/now-recently-previously.html` equals 1
    - `grep -c 'href="/publications/"' _includes/now-recently-previously.html` is >= 2
    - `grep -c "Medical education" _includes/now-recently-previously.html` is >= 1
    - `grep -c "Precision agriculture" _includes/now-recently-previously.html` is >= 1
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test -f _includes/now-recently-previously.html && grep -q "<h3>Now</h3>" _includes/now-recently-previously.html && grep -q "<h3>Recently</h3>" _includes/now-recently-previously.html && grep -q "<h3>Previously</h3>" _includes/now-recently-previously.html && [ "$(grep -c "href=\"/publications/\"" _includes/now-recently-previously.html)" -ge 2 ]'</automated>
  </verify>
  <done>Three-block widget include exists with all three labelled blocks linking to /publications/.</done>
</task>

<task type="auto">
  <name>Task 3: Create _includes/news-widget.html that renders _data/news.yml</name>
  <read_first>
    - CLAUDE.md (Liquid / Data File Conventions — news lives in _data/news.yml)
    - .planning/REQUIREMENTS.md (NEWS-02 — most recent 5 entries; NEWS-04 — at least 3)
  </read_first>
  <files>_includes/news-widget.html</files>
  <action>
    Create a Liquid include that iterates `site.data.news` (expected shape: list of objects with `date` and `item` keys — Plan 02-08 authors this file). Render the latest 5 entries. Since `_data/news.yml` is owner-authored in reverse-chronological order, the include should NOT re-sort by date (avoids forcing a date format). Use `limit: 5`:

    ```liquid
    {% if site.data.news and site.data.news.size > 0 %}
    <ul class="news-list" markdown="0">
      {% for entry in site.data.news limit:5 %}
        <li><strong>{{ entry.date }}</strong> — {{ entry.item }}</li>
      {% endfor %}
    </ul>
    {% else %}
    <p><em>No news yet.</em></p>
    {% endif %}
    ```

    Do not add custom styling — Phase 3 handles theme.
  </action>
  <acceptance_criteria>
    - `test -f _includes/news-widget.html`
    - `grep -c "site.data.news" _includes/news-widget.html` is >= 1
    - `grep -c "limit:5\|limit: 5" _includes/news-widget.html` is >= 1
    - `grep -c "entry.date" _includes/news-widget.html` is >= 1
    - `grep -c "entry.item" _includes/news-widget.html` is >= 1
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test -f _includes/news-widget.html && grep -q "site.data.news" _includes/news-widget.html && grep -qE "limit:\\s*5" _includes/news-widget.html && grep -q "entry.date" _includes/news-widget.html && grep -q "entry.item" _includes/news-widget.html'</automated>
  </verify>
  <done>News widget include reads site.data.news, limits to 5, gracefully handles empty case.</done>
</task>

</tasks>

<verification>
ROADMAP success criterion 2 (landing page identity + bio + Now/Recently/Previously widget) and partial criterion 5 (news widget on landing page) are reachable after this plan plus Plan 02-08 (news data).
</verification>

<success_criteria>
- _pages/about.md renders Harry's real bio with required strings
- Now/Recently/Previously widget exists with three blocks
- News widget exists and references _data/news.yml
</success_criteria>

<output>
Create `.planning/phases/02-content-population/02-02-SUMMARY.md` when done.
</output>
