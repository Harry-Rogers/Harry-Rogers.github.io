---
phase: 02-content-population
plan: 08
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - _data/news.yml
autonomous: true
requirements:
  - NEWS-01
  - NEWS-02
  - NEWS-03
  - NEWS-04
must_haves:
  truths:
    - "_data/news.yml exists"
    - "It contains at least 3 milestone entries"
    - "Each entry is a milestone (paper, talk, award, joining/leaving) — no opinion content"
    - "Entries are in reverse-chronological order"
    - "No _posts/ directory and no _news/ collection exist (structural enforcement)"
  artifacts:
    - path: "_data/news.yml"
      provides: "News data file consumed by _includes/news-widget.html"
      contains: "date:"
  key_links:
    - from: "_data/news.yml"
      to: "_includes/news-widget.html"
      via: "site.data.news"
      pattern: "site\\.data\\.news"
---

<objective>
Create `_data/news.yml` with at least 3 milestone seed entries, all real (not lorem). Reverse-chronological. Structurally enforces "no blog" — no markdown files, no body field, just `date` + `item`.

Purpose: NEWS-01..04. ROADMAP criterion 5.
Output: 1 YAML file.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create _data/news.yml with milestone seed entries</name>
  <read_first>
    - .planning/REQUIREMENTS.md (NEWS-01..04, especially NEWS-03: milestones ONLY — no opinions)
    - .planning/PROJECT.md (Owner Profile — current role, Published Work list for paper dates, Awards: Best Paper KDIR 2023, Best Application nomination TAROS 2023)
  </read_first>
  <files>_data/news.yml</files>
  <action>
    Create `_data/news.yml` as a YAML list of objects, each with two keys (`date` as human-readable string, `item` as one-line markdown-allowed text). Reverse-chronological. Minimum 5 entries (NEWS-02 says widget shows up to 5; NEWS-04 says at least 3 must exist — produce 5 to fill the widget):

    ```yaml
    # News — milestone updates only.
    # Schema: each entry is { date: "<Mon YYYY>", item: "<one-line markdown>" }.
    # NEVER add personal/opinion entries. No body field — structurally enforces "no blog".

    - date: "May 2026"
      item: "Started as Postdoctoral Research Associate at the [Institute of Biomedical Engineering](https://eng.ox.ac.uk/), University of Oxford, in Prof. Alison Noble's Biomedical Image Analysis group."

    - date: "Jun 2025"
      item: "Co-authored paper *Quality Assurance for Home Spirometry using Machine Learning* presented at [IEEE ISCC 2025](/publication/2025-06-08-ieee-iscc-spirometry-qa)."

    - date: "Sep 2024"
      item: "Co-authored paper *Automating the Clock Drawing Test with Deep Learning and Saliency Maps* presented at [EPIA 2024](/publication/2024-09-01-epia-clock-drawing)."

    - date: "Feb 2024"
      item: "Paper *Advancing precision agriculture: domain-specific augmentations and robustness testing* published in [Neural Computing and Applications](/publication/2024-02-01-ncaa-domain-specific-augmentations)."

    - date: "Nov 2023"
      item: "🏆 Best Paper Award at [KDIR 2023](/publication/2023-11-01-kdir-interpretable-quantized-cnn) for *Evaluating the use of interpretable quantized convolutional neural networks*."
    ```

    Use month-precision dates only (NEWS-03 forbids opinion entries; date precision is not part of the requirement). Keep items to a single line each. Mark any unverified date with a TODO comment ABOVE the entry, e.g. `# TODO(owner): confirm exact month`.

    Do NOT create `_news/` directory. Do NOT recreate `_posts/`.
  </action>
  <acceptance_criteria>
    - `test -f _data/news.yml`
    - `! test -d _news`
    - `! test -d _posts`
    - YAML parses: `python3 -c 'import yaml; d=yaml.safe_load(open("_data/news.yml")); assert isinstance(d, list) and len(d) >= 3' 2>/dev/null` succeeds (or use `ruby -ryaml -e` if Python YAML unavailable)
    - At least 3 entries: `grep -c "^- date:" _data/news.yml` is >= 3
    - Each entry has an `item:` field: count of `^  item:` lines equals count of `^- date:` lines
    - No body/markdown opinion text: `! grep -E "I think|I feel|opinion|thoughts on" _data/news.yml`
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test -f _data/news.yml && ! test -d _news && ! test -d _posts && [ "$(grep -c "^- date:" _data/news.yml)" -ge 3 ] && [ "$(grep -c "^- date:" _data/news.yml)" = "$(grep -c "^  item:" _data/news.yml)" ] && ! grep -qE "I think|I feel|opinion" _data/news.yml'</automated>
  </verify>
  <done>_data/news.yml exists with >= 3 milestone entries, no _posts/ or _news/, no opinion content.</done>
</task>

</tasks>

<verification>
NEWS-01..04 reachable. ROADMAP criterion 5 (news widget shows >= 3 milestone entries from _data/news.yml, no _posts/, no _news/) satisfied once Plan 02-02 wires the include.
</verification>

<success_criteria>
- _data/news.yml exists with 3+ milestone entries
- No _posts/, no _news/ directory
- No opinion content
</success_criteria>

<output>
Create `.planning/phases/02-content-population/02-08-SUMMARY.md` when done.
</output>
