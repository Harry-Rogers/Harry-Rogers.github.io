---
phase: 02-content-population
plan: 09
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - _data/navigation.yml
autonomous: true
requirements:
  - POLISH-06
must_haves:
  truths:
    - "Top navigation has no CV entry"
    - "Top navigation lists Publications, Talks, Teaching, Portfolio, plus About if academicpages default includes it"
    - "Removing CV from nav matches the deletion of _pages/cv.md / cv-json.md in Plan 02-01"
  artifacts:
    - path: "_data/navigation.yml"
      provides: "Site nav with no broken CV link"
  key_links:
    - from: "_data/navigation.yml"
      to: "existing _pages/*"
      via: "url: keys"
      pattern: "url:"
---

<objective>
Remove the "CV" entry from `_data/navigation.yml` so the masthead does not link to a deleted page. CV is deferred to v2 per PROJECT.md (POLISH-06 + the constraint "site should not include a broken link").

Purpose: Phase 2 deletes CV surface files (Plan 02-01); this plan removes the nav entry that would otherwise 404.
Output: Updated _data/navigation.yml.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@_data/navigation.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove CV entry from _data/navigation.yml</name>
  <read_first>
    - _data/navigation.yml (existing academicpages nav)
    - .planning/REQUIREMENTS.md (POLISH-06 wording; also POLISH-08 reference — POLISH-08 belongs to Phase 4 but the nav delete portion of CV removal is here)
  </read_first>
  <files>_data/navigation.yml</files>
  <action>
    Open `_data/navigation.yml`. Find any block whose `title` is `"CV"` (or `"cv"`, `"Curriculum Vitae"`) or whose `url` is `/cv/` or `/cv-json/`. Delete the entire entry (the `- title:`/`url:` pair).

    Preserve the other nav entries (typically: Publications, Talks, Teaching, Portfolio/Projects, Blog Posts if present — but Blog Posts entry must ALSO be removed since `_posts/` was deleted in Plan 02-01).

    Also remove any "Blog Posts" or `/year-archive/` nav entry if present, since the posts directory was deleted.

    Final nav should be (or a subset of) Publications, Talks, Teaching, Portfolio. Keep the file as valid YAML; preserve top-level structure (academicpages uses `main:` as the top key).
  </action>
  <acceptance_criteria>
    - `test -f _data/navigation.yml`
    - `! grep -iE "title:\\s*\"?(cv|curriculum vitae)\"?" _data/navigation.yml`
    - `! grep -E "url:\\s*\"?/cv" _data/navigation.yml`
    - `! grep -iE "title:\\s*\"?Blog Posts\"?" _data/navigation.yml` (since _posts/ removed)
    - File still parses as YAML: `python3 -c 'import yaml; yaml.safe_load(open("_data/navigation.yml"))' 2>/dev/null` succeeds (or equivalent ruby check)
    - At least 3 nav entries remain (Publications + Talks + Teaching + Portfolio expected): `grep -c "^  - title:" _data/navigation.yml` is >= 3
  </acceptance_criteria>
  <verify>
    <automated>bash -c 'test -f _data/navigation.yml && ! grep -iqE "title:\\s*\"?(cv|curriculum vitae)\"?" _data/navigation.yml && ! grep -qE "url:\\s*\"?/cv" _data/navigation.yml && ! grep -iqE "title:\\s*\"?Blog Posts\"?" _data/navigation.yml && [ "$(grep -c "^  - title:" _data/navigation.yml)" -ge 3 ]'</automated>
  </verify>
  <done>Nav has no CV entry, no Blog Posts entry, retains at least Publications/Talks/Teaching/Portfolio.</done>
</task>

</tasks>

<verification>
POLISH-06's nav-side requirement satisfied (file deletion happens in Plan 02-01).
</verification>

<success_criteria>
- _data/navigation.yml contains no CV or Blog Posts entries
- Remaining entries are real, non-broken
- File is valid YAML
</success_criteria>

<output>
Create `.planning/phases/02-content-population/02-09-SUMMARY.md` when done.
</output>
